# 変更の記録

バージョン名の付け方は [`js/00-version.js`](js/00-version.js) の先頭に書いてある。
かいつまむと `大版.柱.節.変更` の4組で、

```
00.005.000.000
^^ ^^^ ^^^ ^^^
 │   │   │   └─ 変更 … 1つの変更（コミット1つ）ごとに +1
 │   │   └───── 節 … まとまった作業が終わるごとに +1。ここでアーカイブを控える
 │   └───────── 柱 … 新しい仕組みが1本立つごとに +1
 └───────────── 大版 … 保存データやファイル構成の互換が切れたら +1
```

節が閉じるとき、その見出しに**そのときのコミットのハッシュ**を書く。これがアーカイブで、
`git checkout <ハッシュ>` か GitHub の `/archive/<ハッシュ>.zip` でその版がそのまま動く
（なぜタグではなくハッシュなのかは README.md の「バージョン管理」を参照）。

新しいものが上。数字の意味と実測値は README.md に詳しく書いてある。

---

## 00.013.005.025 — 手で飛ばす補助（離したら姿勢を保つ・旋回半径のレバー）／右のメニューをタブに（現在）

- 操縦桿（昇降舵・補助翼）を離したら、離した瞬間のピッチ角とバンク角を保つようにした。保つのに要る舵とトリムは自動で当てる
  （「飛行」タブの「操縦」で切れる）。ヘリは離したときの姿勢を指示し続ける。
- 操縦のしかたに「旋回半径」を足した（M キー・タッチの「操縦」ボタン・メニューで切り替え。キーボードとタッチの両方）。
  - 補助翼のレバー：高度を変えずに左右へ水平旋回する。100%がその速さでの最小旋回半径。倒しっぱなしでもロールし続けず、
    離すと翼を水平に戻す。旋回で要る揚力と横滑りは、昇降舵とラダーが自分で合わせる。
  - 昇降舵のレバー：上下に曲がる（100%で最小半径の宙返り）。離すと、そのときの上昇角（ほぼ水平なら高度）を保つ。
  - 翼が失速する手前（機体ごとに測る）で引くのを止める。いまの最小旋回半径を計器とメニューに出す。
  - 実測（補助翼100%で60秒）：高度のずれは練習機1〜5m・747 1〜2m・Concorde 4〜19m・TB1新 2〜7m など。
- 舵を決めるのを1/60秒ごとにした（1フレームが長い端末では1フレームを刻む）。画面が重いと、舵の輪が暴れていた。
- 右のメニューを「飛行・自動操縦・地図・天気・設定」のタブに分けた（選んだタブは覚える。地図で着陸地点を選ぶときは地図のタブへ移って戻る）。
- 飛行・自動操縦の注記が本文より大きな文字で出ていたのを直した。
- 検証 `tools/verify-flight.js` に補助操縦の項目を足した。

## 00.013.005.024 — 首府の巨大都市を直径20〜40kmに・まわりに数百kmの都市群（メガロポリス）を作る

- 首府の巨大都市（10都市）の市街地を直径22〜40kmにした（以前は7km前後）。均すのは都心だけで、外は地形なりに広がる。
- 巨大都市を軸に、同じ国の大都市や街が一列に並ぶ都市群（メガロポリス）を作った。10か所・端から端まで116〜337km
  （いちばん長いのは Greater Oberfield の10都市・337km）。仲間の間が空いているところには衛星都市を置き、都市は130から166になった。
- 都市群の帯（幅12〜16km前後）の中は、地表が市街地の色になり、森が生えず、家・倉庫・中層の建物と街路が続く。
- 巨大都市の外側と帯は1.6km四方の区画に分け、見ている場所からの距離で詳しさを変えて建てる（5km以内は家まで全部・
  12kmまでは14m以上・26kmまでは高層ビルだけ）。その先は都市群まるごとの灯りで、夜は数百kmの光の帯が見える。
- 遠い区画の灯りは間引いて点を大きくした（遠くの点が1画素で描かれて明るすぎた）。
- 地図に市街地の灰色と、都市圏の名前（Greater Oberfield など）を帯に沿って出す。
- 検証 `tools/verify-world.js` に都市群と区画の項目を足した。

## 00.013.005.023 — 自動操縦：前進切替の推力・巡航の速さ・旋回の半径を直す／Builderでヘリを作りやすく

- 垂直離陸から前へ進むときの出力を、エンジングループのはしご（遅いグループから順に上がる）を踏まえて解くようにした。TB2は以前、小さいジェットにしか出力が渡らず（重さの3%）355ktから加速しなかった。いまは58秒で上昇へ移る。
- 巡航が最高速度まで出るようにした（推力の上限を「抗力を超えるぶん」で決める・減速の見積もりを抗力で出す）。TB1新はTHCA→ORKU（目標3000m）でマッハ5.8頭打ちだったのが、マッハ9.4（高度3000mで推力と抗力が釣り合う速さ）のまま残り590kmまで飛ぶ。
- 旋回を小さく・速くした（旋回の終わりを速さによらず8秒で詰める・速度に余裕があれば深く倒す）。180°向きを変えるのに、747が149秒→31秒、Concordeが228秒→29秒、TB2新が150秒→39秒。
- スポイラーで失う揚力を重さの1倍までに抑えた（マッハ9で立てて-26Gまで振れていた）。
- ヘリ：重心がローターの軸から少しずれていても（直径の5%まで）姿勢を保てるようにした。横向きのエンジン（尾部ローター）は推力を出さない。
- Builder：ヘリの重心を「ローターから決定」、ローターの推力を「最高速度相応の出力へ」のボタンを足した。

## 00.013.005.022 — 設定のダウンロードと読み込みに3Dモデルを含め、まとめてZIPにする

- Builderの「設定をダウンロード」が、設定と3Dモデルを1つのZIPにするようになった。「設定を読込」はZIPなら3Dモデルごと開いて保存にも入れる（設定だけの.jsonも今までどおり読める）。
- Builderに「まとめてZIP」を足した。保存済みの機体すべてと飛行画面の設定を1つのZIPにする。読み込むと全機が保存に入る。
- 飛行画面にも「機体もまとめて書き出す（.zip）」を足し、「設定を読み込む」でZIPも読めるようにした。取り込んだ機体はその場で一覧に出る。
- ZIPは自前で読み書きする（ブラウザの圧縮を使う。外部ライブラリなし）。6.4MBのモデルが1.6MBになり、1バイトも変わらずに戻る。
- 検証 `node tools/verify-pack.js` を足した。

## 00.013.005.021 — ヘリコプターを足す

- エンジン種別「ヘリのローター」を足した（Builderで選べる。ローターの直径も入れられる）。ローターだけで浮く機体はヘリコプターとして飛ぶ。
- 飛行画面の機体一覧に内蔵のヘリコプター（2,400kg・直径11m）を足した。推力は重さの1.38倍、上がれるのは標高2,200mまで。
- 出力（Shift/Ctrl）がコレクティブ。操縦桿は姿勢の指示で、放すと水平に戻る（前後±25°・左右±35°）。Q/Eで向きを変える（±45°/s）。
- ローターの効きは、転移揚力（前へ進むと最大+18%）・後退側の羽根の失速（72m/sから）・地面効果（最大+12%）で変わる。
- 全自動（I）はヘリ用の飛び方：真上に上がって、地形から150m以上を巡航し、減速しながら降りて真上から接地する。30km・横風8m/sで降り場から0.3m、接地-0.40m/s。ホバリング（J）と高度維持（O）もヘリ用。
- メインローター・尾部ローターが回る見た目と、ばたつきのあるローター音。

## 00.013.005.020 — 太陽の影を付ける

- 太陽に影の地図を付け、機体・建物・家・木・港・空港の建物・橋が地面や建物に影を落とすようにした。
- 影を描くのは見ている場所のまわりだけ（視点の遠さで±250〜900m）。近くで機体を見ると1画素0.24mで細かく出る。
- 画質「高」2048画素・「中」1024画素、「低」と夜は描かない。パネルの「影を落とす」で切れる。

## 00.013.005.019 — 海の近い街に港を作る

- 海の近い街（中心から20km以内に海）の海岸に港を置き、街から道路の支線を引く（26か所）。
- 岸壁は岸から海へ突き出した床（地形は均さない）。ガントリークレーン・コンテナ・倉庫・照明塔・防波堤・灯台・
  横付けしたコンテナ船とばら積み船。夜は灯りが点く。
- 検証：港の付け根が低い陸・沖が深い海・道が付け根まで・空港と街から離れている。

## 00.013.005.018 — 橋の形を長さで変える（アーチ橋・斜張橋・吊り橋）

- 渡る長さで橋の形を選ぶ：〜250m 桁橋、〜520m アーチ橋、〜820m 斜張橋、それより長いと吊り橋。
  道路の橋43本はアーチ11・斜張27・吊り5。アーチ・斜張・吊り橋は川の中に橋脚を立てない（塔の下だけ）。

## 00.013.005.017 — 街を規模・山あい・気候で作り分け、真ん中に名所を建てる

- 街ごとに規模（町・市・大都市・巨大都市）、山あいかどうか、気候（寒冷・温帯・乾燥・熱帯）を決める。
  町は家ばかり、大都市は中心に高層ビル、巨大都市は超高層ビル（最高393m）と展望塔。山あいの街は低く、屋根が急。
- 屋根を付けた（切妻・寄棟・平屋根。気候で変わる）。高層ビルは段々に細くなり、窓明かりと赤い航空障害灯。
- 街の真ん中に国ごとの名所：尖塔の教会・丸屋根・丸屋根と尖塔・五重塔・鐘楼。まわりは広場。
- 建物は500軒ずつ何フレームかに分けて作る（形を付けて重くなったぶん）。
- 検証：規模4段階、高層ビルは大都市・巨大都市の平地だけ、町の建物は家、山あいに20m超なし、名所が全都市・舗装にかからない。

## 00.013.005.016 — 部品ごとに「飛行画面でも仮モデルを出す」を選べるようにする

- 脚・エンジン・翼・舵面のプロパティに「飛行画面でも仮モデルを出す」を足した。オンの部品は、
  Builderで見ている仮の形（銀色の脚・エンジンの筒・翼の板・舵面の板）が飛行画面の機体にも出る。
- 飛行画面では、脚は上げ下げ（G）で4秒かけて格納・展開し、舵面は操縦のとおりに振れる
  （前後を反転させたモデルでも同じ向き）。見た目だけで、飛び方と当たり判定は変わらない。
- 仮モデルの形の作り方を js/env/09e-part-proxy.js にまとめ、Builderもそこを呼ぶようにした。

## 00.013.005.015 — 横風の離陸滑走で滑走路から外れないようにする

- 手で滑走しているとき、ラダーに触っていないあいだは滑走を始めた線を保つ（ヨーダンパーは振れを止めるだけで、
  機首を取られた向きのまま斜めに走って滑走路を出ていた）。ラダーを当てればそのとおり曲がり、離したところで取り直す。
  画面で横風8m/s：TB2_18 52.6m → 2.0m、TB1_21 5.1m → 0.9m。練習機（横風8±3m/s）83.4m → 4.8m。
- 自動の滑走の中心線保持に、タイヤのすべり角と方位のずれの積分を足し、効きを10倍に。
  横風12m/sで Concorde 67m → 2.0m、TB2_18 47m → 2.6m、TB1_21 15m → 3.7m。
- Boeing 747（アップロードのデータ）は止まっていても前輪が浮いている（重心が前側の主脚より後ろ）ので、前輪で曲げられず直らない。

## 00.013.005.014 — 計器を半分くらいにして、画面の大きさに合わせて縮める

- 対気速度・高度・出力などのタイルと水平器を、画面の幅と高さの小さいほうに合わせて縮める。
  広い画面でも以前の半分くらい（タイル 86×50px → 46×32px、数字 20px → 9〜12px、水平器 150px → 56〜80px）。
- 操縦装置を出しているときは下段の計器を上段のすぐ下へ移す（縦長の画面でボタンと重なっていた → 重なり0）。

## 00.013.005.013 — ホーム画面に追加してアプリのように全画面で開けるようにする

- マニフェスト・サービスワーカー・アイコンを足した。Android の Chrome は「アプリをインストール」で入れられ、
  ホーム画面から全画面で `flight.html` が開く。iPhone・iPad は「共有 → ホーム画面に追加」。
- 上の帯に「📲 アプリにする」（入れる／手順を出す）と「⛶ 全画面」（ブラウザのまま全画面）を足した。
- サービスワーカーはネットワーク優先（更新がすぐ届く）。つながらないときだけ前のファイルで動く。

## 00.013.005.012 — 機体を替えると全自動を入れても動かないのを直す

- 前の機体で止めたエンジングループの記録が残り、次の機体のエンジンが止まったままになっていた
  （TB2_18の自動離陸のあとTB1_21に替えると推力0）。滑走路へ戻すたびに取り直し、
  自動操縦もグループが1つだけの機体でそれが止まっていたら点け直す。

## 00.013.005.011 — 離陸の首振りを止める、機体を切り替えると出力が上がらないのを直す

- 飛行中に機体を選び直すと、フォーカスがセレクトに残って操縦のキー（Shiftの出力など）が効かなかった。
  飛行中はセレクト・チェックボックス・スライダーのフォーカスを外してキーを操縦に回す。機体と視点のセレクトは選んだら外す。
- 自動離陸で浮いたあとと上昇の段にヨーダンパーを掛ける（横風で浮いた直後に±1°/s・周期1.6秒で首を振っていた）。
- 手で飛ばしているときも、ラダーに触っていないあいだはヨーダンパーを効かせる（地上の滑走でも）。
  横風4m/sでTB1_21が滑走中に±1.2°/s・周期2秒で左右に振れていた → 振れない。
- 海の波の向きを風下へ直す（風向きの向きの取り方を逆にしていた）。

## 00.013.005.010 — 地図をドラッグ・拡大（空港の形・道路・街路）、垂直着陸の降り場と着陸後の垂直エンジン

- 地図をドラッグで動かせるようにする（飛行中も）。「現在地」で機体へ戻る。ホイール・2本指のピンチで一辺4km〜1,400km、
  ズームの段に「近郊（40km）」「詳細（6km）」を足す。寄ると道路・街路・空港（滑走路・誘導路・エプロン・ターミナル）の形を描く。
  地図で着陸地点を選ぶときは、右のメニューを閉じていても開く。
- 垂直着陸なら長い帯は探さず、選んだ所のすぐそばの機体が収まる平らな所（40〜84m四方）に降りる（丘陵で6.4km→120m）。
- 垂直着陸のあと、垂直エンジンを抜ききってから「着陸しました」にする（TB1_21は61%・TB2_18は76%のまま点きっぱなしで、
  風8m/sの中60秒で587m・164m流されていた → 0.2〜1.0m）。
- 後ろへ転がっているあいだは車輪で止めない（尾輪の無い機体）。一度止まったら駐機ブレーキを掛けたままにする。
- 垂直降下の地面近くでは、風に逆らう傾きを中心に水平へ戻す。降りられないまま8秒たったらゆっくり降ろしきる。
- ホバーの姿勢を変える力を、垂直エンジンが支えている重さの割合で決める（推力が桁外れな機体は傾けられなかった）。
- 検証：地図で選んだ地点への垂直着陸（平野・丘陵・横風）、着陸後に垂直エンジンが止まり流されないことを追加。
  脚を後ろ寄りにした垂直着陸の試験機は、前脚ごと下げて立っていられない形になっていたので主脚だけ下げる。

## 00.013.005.009 — 海に波ときらめき、河口と三角州を海までつなぐ

「地形と海の見た目」から（重めの項目の最後）。

- 海に波・空の映り込み・太陽のきらめきを付ける（`applyWaterSurface`）。周期16m〜1024mの4枚の波を風下へ流して重ね、
  フレネルで空の色を映し、波ごとに太陽を鋭く映す。川と湖も同じシェーダー・同じ色にして、河口の色の境を消した。
- 河口を、街・空港のアンカーで沖へ出た海岸線まで伸ばす（68の河口のうち41が最大25km手前で終わっていた）。
- 川面を、海面より下でも両岸が陸のあいだは海面の高さで張る（細い谷が地形のLODに出ず、川が途切れて見えた）。
  三角州の分流にも川面を張り、堆積の外まで伸ばす。
- 三角州の縁は海面を急にまたがせる（縁の海面すれすれの平地が海面と重なってちらついていた）。
- 検証：河口が海岸まで届く（68/68）・三角州の縁に海面すれすれの平地が無い、を追加。

## 00.013.005.008 — 超高速機（サンダーバード1号・2号）の着陸を直す

TB1_21・TB2_18 の「接地しても浮いたまま・転倒する」「跳ね返されて上昇し続ける」の報告から。

- 着陸滑走でフラップを戻す（全開のままだとTB2が接地後に起き上がって上昇し続けた）。
- タイヤの横の力をすべり角に比例させる（8°で上限）。0.2°のずれで上限に達していて、TB1が横風で裏返った。
- 引き起こしの最後は一定の沈下率で降ろしきる。速い機体ほど引き起こしを短く強くする（下限0.8倍）。
- 逆噴射は滑走路の残りで止まれるぶんまで入れる（3〜6m/s²）。
- 最終進入の中心線への寄せを旋回半径に合わせて緩める（TB2が±300mで蛇行して滑走路の外に接地した）。
- 地上で中心線へ戻る横の速さを秒速4mまでにする（高速で急に戻ってTB1が転がった）。
- 実測：TB1_21 -318fpm・停止1394m、TB2_18 -286fpm・停止1601m、旧TB1 停止2409m（以前は走り越し）。
  画面の世界でも両機とも止まってターミナルまで走る。

## 00.013.005.007 — 地図で選んだ地点のそばの平地へ自動で降りる

「選んだ地点の最寄りの平地への自動着陸」から（重めの項目の続き、4つめ）。

- 「🗺 地図で着陸地点を選ぶ」を押して右の地図をクリックすると、そのまわりで機体が降りられる長さの
  まっすぐ平らな帯（陸・勾配0.8%以下・凹凸2m以内・横1.2m以内・街や空港の外・木がまばら）を探し、目的地にする。
  地図には帯を桃色の線で出す。
- 帯を滑走路1本の空港に見立てて、進入・着陸滑走・垂直着陸をそのまま使う。降下の経路は接地点の地面の高さへ引き、
  垂直着陸は帯の中心に降りる。
- 帯の長さは進入速度から見積もる（練習機970m〜TB1 5000m、垂直着陸600m）。
- 実測で練習機・三式戦・747・Concorde・TB2・TB1（垂直）の全機が、平野と丘陵の両方で帯の上に降りて止まる。
- 検証：本物の世界の地形で平地へ降ろす試験を追加。

## 00.013.005.006 — 湖の岸を地形の等高線にする

「地形に合った形の湖」から（重めの項目の続き、3つめ）。

- 湖を置く場所に滑らかな浅い椀を引き、その上で低いところから水を満たして（priority flood）、
  あふれ口か予定の広さに達した高さを水面にする。岸は地形の等高線になり、谷に入り込む入り江・
  尾根の半島・島ができる（岸の入り組み方 中央2.65、真円は1.62）。
- 水面のメッシュは水の升目を覆う板にし、ミニマップは方位ごとの岸までの距離で描く。
- 川床は湖の椀を引いた地面で決め、湖の中は湖面で頭打ちにする。湖のそばでは川の谷が湖面より下を削らない。
- 検証：岸の升目で「水面が浮いていない」を見る形に直し、岸の入り組み方を追加（島は構わないので島の検査は外した）。
- 世界の生成は約2.0秒→約2.2秒。

## 00.013.005.005 — 街路の形を街ごとに変える

「碁盤の目でない街路と、街ごとの違い」から（重めの項目の続き、2つめ）。

- 街路網を型から作る（js/env/03j-city-layout.js）：碁盤の目・うねった碁盤の目・環状＋放射・
  旧市街の核＋碁盤の目・曲がりくねった路地。型は国の性格で重みを付けて街ごとに選ぶ
  （26 / 31 / 27 / 29 / 17 都市。4都市以上ある10か国すべてで2種類以上）。
- 建物はいちばん近い街路に面して、その向きに揃えて建てる（以前は世界の軸のまま）。予定の96%が建ち、舗装にかかる建物は0軒。
- 建物の色・高さ・密度を国ごとに変える。
- 1都市を「並べ方」「建物」「街路」の3つの仕事に分けて別々のフレームで建てる。
- 世界側の碁盤の目専用の worldCityStreetDist と街路の幅の定数を削除（使われていなかった）。
- 検証：街路の型の数・国ごとの型の混じり方・建った軒数・舗装にかかる建物・並べ方の時間を追加。

## 00.013.005.004 — 川を山から始める

「山から始まる川」から（重めの項目の続き、1つめ）。

- 谷底を遡って止まったところがまだ山の中でなければ、いちばん近い山脈の稜線へ向けて遡りを続ける。
  山脈が遠い海岸平野の川は近くの丘を目指し、それでも源流が300m未満の川は別の河口で引き直す。
  源流の標高は中央値393m→833m、300m未満25本→0本、1000m以上9本→20本、68本中42本が山脈の中から。
- 谷の形を区間ごとに決める（谷底600mは0.03のまま、その外の谷壁と上流側の端の先を必要なだけ急に）。
  急な川で下流の谷が上流の川床を削り、川面が地面から浮いていた（以前から1128点・最大558m → 10点・最大11m）。
- 山の中の階段状の川床の角を丸め、川面だけ深度を手前へ寄せる（水面の縁がのこぎり状に欠けていた）。
- 道路の橋：水の上だった区間の水面を2m刻みで測り直す（桁と水面の差が5.95mの所があった）。
- 検証：源流の標高・山脈の中から始まる本数・川面が浮かないことを追加。
- 世界の生成は約1.4秒→約2.0秒（川が長くなり、平野から始まる川を引き直すぶん）。

## 00.013.005.003 — 自動離陸でまっすぐ上がってから曲がる

「サンダーバード1号が自動で離陸する瞬間、自分から進路を曲げる」から。

- ラダーではなく、浮いた3秒後・対地300mで目的地へバンク84°で倒し込んでいたのが原因。
  目標高度の4割（450〜1500m）までは滑走路の延長線に沿って上がり、そこから旋回を少しずつ戻す。
  TB1は方位249°のまま対地1636mまで上がってから曲がるようになった。
- 指示より大きく沈んでいるときはバンクを浅くする（旋回に入ったあとバンク±85°で墜落していた）。
- 上昇・巡航・降下の推力を重さの3倍までにした。TB1が垂直離陸のあと1.5秒で474kt→3018ktになり、
  高度100km超まで上がっていった。推力重量比3未満の機体には効かない。
- 垂直離陸の前進切替の出力を、加速が重さの0.35倍までに（全開で7000ktのまま地面に突っ込んでいた）。
- 検証：「沈み始めたら出力を戻す」をレバーの量ではなく推力（上限いっぱい・重さ以上）で見るようにした。
- 回帰：やり直し・墜落0のまま。三式戦 1177→1392秒、TB2 438→494秒（まっすぐ上がるぶん遠回り）。

## 00.013.005.002 — 着陸後にターミナルまで地上走行する

「着陸後にターミナルまでの地上走行」から（重めの項目の3つめ）。

- 全自動で着陸したら、減速して8m/sを切ったところで地上走行に入り、滑走路の出口 → 平行誘導路 →
  エプロン → 駐機場所（ピアとピアのあいだ）まで走って、機首をターミナルへ向けて止まる。
- 舵は純追従（道筋の少し先の点へ届く円から前輪の切れ角を決める）。見る先は最小回転半径より近くしない。
  速さは直線8m/s・曲がり角3m/s。出力は推力と重さの比で合わせる。尾輪式はブレーキを加減する。
- 主脚が前後に何列もある機体は、低速で主脚も曲がる円に沿わせる（747が誘導路の角を曲がれなかった）。
  実機6機の全自動の飛行の数字は変わらない。
- 実機6機が模型の空港で止まる点から3.0〜4.2m以内に着く。`tools/verify-flight.js` に (16) を足した。

## 00.013.005.001 — 道沿いに家を建て、道の上に木を生やさない

「道沿いの家」から（重めの項目の2つめ）。

- 道沿いに家を建てる。村のまとまり（道の長さの26%）と郊外（市街地の外2.5km）では40mおきの候補の7割に、
  ほかはまれに1軒。壁と切妻屋根、棟は道に沿う。夜は窓に灯り。置き場所はハッシュで決まり、作り直しても変わらない。
- 水の上・橋のたもと・空港・ほかの道の上・急な斜面・市街地には建てない。カメラから14km以内だけ。
- 道の上に木を生やさない（道の縁から10m、遠景の森の塊は18m）。以前は道のまん中に木が立っていた。

## 00.013.005.000 — 橋を架ける

アーカイブ：この節（00.013.005）はまだ開いている（節が閉じるときにハッシュを控える）

「橋」から（重めの項目の1つめ）。

- 道路が川・湖を渡るところに橋を架ける。桁は水面+6m、両岸は勾配6%の取付け部（盛土・擁壁）。
  桁の厚み・欄干・48mおきの橋脚。街灯は桁の上に立てる。橋31本・桁の総延長19.1km。
- 川に沿って水の上を行く道（3.6km・2.4km）は、道と直角の向きで近いほうの岸へ出す。
- 街の中の街路は、川を横切るところの3本に1本を橋にする（桁は水面+4m）。
- `tools/verify-world.js` に「川・湖の上で橋桁の上にある道路」「川に沿って縦に行く橋が無い」を足した。

## 00.013.004.013 — 着陸灯で海面・雲底・積雲を照らす

「雲、雲底とか、海面もしっかり着陸灯で照らされて欲しい」から。

- 照り返しの板を「地面・海面・湖面・川面のうち高いもの」に置く（以前は海の上では海底に沈んでいた）。
- 光が雲底に当たるときは、板を雲底の面に置く（雲の上から見下ろす・雲の下から上へ向ける）。
- 光の軸が積雲に入るところに、光の当たった明るい玉を置き、光の筋もそこで止める。
- 雲の中（積雲の玉の中・雲底の面）では、光の筋を霧の中と同じ濃さにする。

## 00.013.004.012 — 太陽・月・星と灯りのにじみの見える順番を直す

「月太陽が地面や雲底を超えて見えたり、滑走路の灯りのブルームが雲底より後ろに見えたり」
「ライトは見えないのにブルームが見えてたり、ブルームが機体にメリ込んでる感じ」から。

- 太陽・月・星を無限遠（深度1.0）に描く。18〜24km先の天球のままだと、それより遠い山や雲底の手前に出て、
  地平線の下の太陽が地面の上に顔を出していた。
- 空港の灯りとにじみを、こちら側の積雲と同じ組で描く。以前は半透明のなかで最初に描かれ、
  手前にあっても雲底や霞に上から塗られていた。
- 航行灯と炎のにじみの深度を玉の半径ぶん手前へ引き、機体にめり込ませない。
- 灯りが機体の陰にあるときは、にじみも消す（カメラから芯の5点へ光線を飛ばして調べる）。

## 00.013.004.011 — 滑走路の舗装・標示を地面ぴったりに敷き、車輪を埋もれさせない

「白線や文字と滑走路面、地面を、重なって見えないように高さをずらしてくれてるけど、車輪が埋もれて見えない。
地面に全てピッタリ揃えて欲しいけど、重なって見えることはないようにして欲しい」から。

- 整地エリア・誘導路・エプロン・滑走路・標示・数字を全部、地面の高さ（0）に敷く。
  以前は0.4〜1.4m浮かせていたので、車輪が1〜1.4m埋まって見えていた。
- 重なりは深度を手前へ引いて解く（`applyDepthPull`）。対数深度バッファでは `polygonOffset` が効かないので、
  シェーダーで深度を書き直す。距離の比率・画面の傾き（画素）・数mm の3つを重ね順ごとに足す。
  大きな板は200m角に割る。
- ちらつき（カメラを1cmずらしたときに変わる画素）は滑走路の上の60m〜90kmで、浮かせていたとき以下
  （60m先 4.41→3.24‰）。
- 着陸灯の照り返しも同じやり方で標示の上に出し、浮かせる高さを1.8m→0.3mに。

## 00.013.004.010 — 地図に国名・国境・都市・空港コード・山・川・湖を載せる

「地図に、国名、空港(略記)、都市、国境、現状の表記のある山、川、湖の名称を表示するようにして欲しい」から。

- 右パネルの地図に、国名・都市・空港コード・山脈・峰・川・湖の名前と、国境（破線）・川筋・湖を描く。
- 大事なものから順に置き、重なるものは書かない（国名 → 選んでいる空港 → 首府 → 山脈 → 峰 → 湖 → 川 → 都市 → 空港コード）。
  川の名前は川筋に沿って傾ける。世界全体で国名10・首府と大きな街・高い峰・主な空港コードが入る。
- 高精細な画面では地図の内部解像度を上げて文字を滲ませない。配置は範囲が変わったときだけ決める
  （最大9.7ms）、毎フレームの描画は最大0.63ms。

## 00.013.004.009 — 街の建物を川の中に建てない

「街の中で川に家が建っている」から。

- 間口の中心と四隅のどれかが水（川・湖・海）に入る建物は建てない。
  以前は 88,443軒のうち 350軒（4都市）が川の上に建っていた → 0軒。建てる時間は変わらない。
- 郊外の灯りを川面に置かない。街路の舗装も川を渡るところで切る（川面の下に透けていた）。

## 00.013.004.008 — 河口を海まで届かせ、湖の水面を浮かせない

「川の河口が海まで少し届いていない」「湖が透けて見えるところがある」「水面の高さがずれる」から。

- 川の水面の帯を、最後に張れた点ではなく海面ちょうどの点で切る。以前は河口で水面が
  海面の0〜2.5m上・最大375m手前で終わり、谷の斜面が乾いたまま海まで残っていた。
- 海の2.6〜16km手前で終わっていた11本の川を、終点から低いほうへ歩かせて海まで下ろす。
  河口は入り江51→62（三角州6はそのまま）。終点に置いていた内陸湖10は無くなり、湖は49→39。
- 湖の水面を中心の地面の高さから**岸のいちばん低いところ**に変える。以前は下り側の岸が
  水面より低く（岸の 4,383/8,820 点、最大248m）、水面が浮いた板になって下が透けていた。
  いまは岸360方位すべてで地面が水面以上（39/39湖）。
- 上り側の岸の崖を、水面から上がる斜面で削ってつなぐ（岸から300mの傾き 最大0.08）。
- 岸の形を1つの関数（`worldLakeRimScale`）にまとめ、掘り方・水面（72→240方位）・判定がこれを見る。
  湖の中の小山で岸を拾い損ねて湖底が剥き出しになることも、outerR の崖も無くなった。
- 川と湖の水面をつなぐ。湖面を川が岸を横切るところの川の水面以下にし、注ぐ川は岸の手前3kmで
  湖面まで下ろし、流れ出る川は湖面より上に出さない（段差 最大29m → 0.00m）。湖の中には川の帯を張らない。
- `tools/verify-world.js` に「水面が浮いていない湖」「水面を突き抜ける地面がない湖」
  「湖の岸の外が崖になっていない」「湖と段差なくつながる川」を足した。

## 00.013.004.007 — 夜の道路を街と同じくらいに見えるように明るくする

「道路を街と同じくらいに見えるように明るくしてほしい」から。

- 街灯を街の灯りと同じ大きさ（22→26）・強さ（0.75→1.0）にし、300m→200mおき、
  26km→55km（道の帯と同じ範囲）まで出す。にじみも 0.16→0.22。
- 夜の路面の自己発光を `0x1c160c` → `0x5a4424`。遠くで街灯の点が消えても、道が光る線として残る。
- 夜22時の画面の明るさ（道路の上／街の上）：中空 15.5／19.4 → 27.2／20.6、
  上空 11.7／34.9 → 15.3／34.9（直す前は上空で道路が周りの地面と同じ11.3だった）。
- 街灯の点は街ひとつのまわりで408→1,211個、道の作り直しは10msのまま。

---

## 00.013.004.006 — 道路と街灯のずれ、尾輪式の操向が逆になる、自動離着陸で横風に流されるのを直す

「道路と街灯の位置（とくに空港の車寄せにつながるところ）で若干ずれてる」「自動の離着陸のとき、
風に煽られて左右にブレるのを、ラダーで抑えて」「TB1みたいに、ノーズコーンのほうが後ろにある
機体が、滑走時に左右移動が反転しちゃう」から。

### 道路と街灯
- 帯は間引いた折れ線、街灯は間引く前の経路に置いていたので、カーブで街灯が道の外に立っていた。
  空港の支線は車寄せへ曲がる取り付き点が間引かれ、帯が角を斜めに切って最大289mずれていた。
- 曲がり角（12°以上）と支線の最後の2点は間引かず、街灯は帯と同じ折れ線の上に置く。
  道幅の外に立つ街灯 135/2,414基 → 0基。

### 尾輪式の操向
- 操向輪が主脚より後ろにある機体（TB1・三式戦闘機）は切る向きを逆にする。
  右ラダーで左へ曲がっていた（TB1 -12.2°・三式 -5.6° → +12.2°・+7.5°）。
- 尾輪式は着陸後のブレーキで前にのめらないよう、尾を押さえ、機首が下がりはじめたら緩める
  （TB1が390ktで踏んでピッチ-82°まで倒れ、裏返っていた。接地19.1G → 4.9G）。

### 横風での自動離着陸
- 地上は中心線を保つ舵（方位のずれ・中心線からのずれ・振れる速さ）に。前輪は速くても±7°残す。
- 接地の直前（対地4m）までクラブで降りる。経路より下ではスポイラーを速さだけで立てない。
- 舵を振り切っていても姿勢が離れていくならトリムを積む（747が横風の離陸でピッチ80°まで
  上がって失速していた）。
- 横風8m/s±3で：離陸滑走の横ずれ 練習機36m→5.7m・Concorde 211m→21.5m・747は失速→離陸、
  接地時のずれ 三式53m→1.4m・747（着けず）→6.8m・Concorde 28m→0.9m。
- 残り：747の離陸滑走38m（機体が69ktで勝手に前輪を上げる）、TB1は横風の中400kt近くで
  接地すると主脚1本で横へ転がる。停止距離も約3.9kmで3,000mの滑走路に収まらない。
- `verify-flight.js` (15)：尾輪式でもラダーの向きに曲がる、横風の離陸滑走で滑走路を外れない。

---

## 00.013.004.005 — ターミナルと管制塔を実際の空港の大きさにする

「管制塔とターミナル、機体のサイズ感に対して小さすぎない？」から。

- ターミナルは 1本 110×26×9m → 220×44×16m、2本 240×42×17m → 520×70×24m、
  3本 340×42×17m → 800×90×28m。ピアも本数・長さを増やした（1本の空港にも1本）。
- 管制塔は一律35m → 42m／78m／100m（羽田116m・ヒースロー87m・フランクフルト70m）。
- エプロンはターミナルの正面に接して、幅はターミナル＋80m。草地も管制塔まで広げた。
- ターミナル・管制塔の上を通る道路は全空港で0本。

---

## 00.013.004.004 — 旋回の大きい機体が、進入で回りきれずに永遠にやり直すのを直す

「一部の旋回の大きい機体が、降下から進入に入ると、回りきれないで永遠とやり直しになる」から。

降下もやり直しも最終進入開始点（FAF）へまっすぐ向かっていたので、滑走路と関係ない方角から
FAFに着き、進入のバンク（20°）では中心線に乗りきれずにやり直し、またFAFへまっすぐ戻って
同じ角度で着く、を繰り返していた。東へ80km飛んで滑走路が逆向きの空港へ入ると、
Boeing 747が17回・Concordeが19回やり直して着陸できず、TB1は真横でも逆向きでも着陸できなかった。

- まっすぐ向かって中心線から30°以内で着けるときはこれまでどおり（正面からの検証は数字まで同じ）。
- そうでなければ中心線をFAFから旋回半径の4.5倍外へ延ばした**入口**へ向かい、滑走路の向きを
  向いて横ずれが旋回半径の1.5倍以内になったら、FAFの点ではなく**中心線そのものへ寄せる**。
- 旋回半径は進入と同じ20°のバンク・「上昇／進入の速度の大きいほうの1.3倍」で見積もり、
  入口を経由しているあいだは巡航・降下・やり直しとも、入口までにその速さへ落とす
  （いまの速さで見積もると、加速するほど入口が遠ざかる循環でTB1が4,000ktを超えた）。
- FAFで機首が45°以上ずれていたら最終進入に入らず入口からやり直す。やり直しは上がりきったら
  残りの道のりで落とせる速さに絞る（全開のままだとConcordeが299ktで進入して10.7G）。
- 6機×（真横・逆向き）の12回すべてが着陸・やり直し0回。`verify-flight.js` (14) を足した。

---

## 00.013.004.003 — 雲底より上の雨、雲底と雲の前後、煙の消え方、コックピットで首を振る、近くの峰の名前

「雨、雪が雲底より上でも降ってるのは修正して」「見上げると、雲底の方が手前に表示されちゃう」
「ロケットの噴煙と飛行機雲の消え方を、霧散していく感じでボワぁ…っと消えるように」
「パイロット視点を、上、左右に首振れるように」「近くを飛んでる時は、小さめだけど明らかに峰の
ある山は、名前を表示して（標高2000メートル以上の山で）」から。

- **雨と雪は雲底より下だけ。** 降水の箱（カメラのまわり150m）の中で、雲底より上の粒を1粒ずつ
  落とす（切り口は0〜30m下へばらす）。以前は「雲底からの高さ×雲量」で全体を薄めていただけで、
  雲量0.6なら雲の上でも4割降っていた。雨・雪・曇りで雲底の上下から測って、雲底より上の粒は0。
- **雲底と雲・雨の前後。** 積雲に描く順番が無く（0）いつも雲底より先だったので、雲底より手前に
  ある雲の下の部分や、見上げたときの手前の雨にまで雲底が上から塗られていた。
  積雲は1つずつ「カメラと同じ側か」で雲底の前後に振り分け、雨と雪は雲底より後に描く。
  べったり曇っているとき（不透明度0.5以上）は雲底が深度も書くので、雲底をまたぐ雲は雲底の面で
  切れ、雲の上から見下ろしたときの灯りや煙は雲底の下に隠れる。
- **煙と飛行機雲の霧散。** 濃さは前半ゆっくり・寿命の45%からなめらかに0まで、大きさは終わり
  ぎわにもう一度膨らみ（1.5倍／1.45倍）、粒ごとにばらばらに流れて筋がちぎれる。
  ロケットの煙の最後の1/5の濃さ 0.24（そこでぷつりと消えていた）→ 0.04。
- **コックピットで首を振る。** 画面をドラッグで左右±150°・上85°・下60°。ダブルクリック／
  ダブルタップで正面へ。首の向きは機体に対して持つ。
- **近くの峰の名前。** カメラのまわり48km四方で、標高2,000m以上・半径1.5kmの輪より150m以上
  高い峰を探して（8km動くたび、1回6〜13ms）、16km以内だけ小さな札を出す。名前は頂の位置から
  決まるので何度来ても同じ。最高峰のまわりで19座、30番目の山のまわりで3座。

---

## 00.013.004.002 — 遠くの森の塊を小さく詰めて、大きな画面で「謎の山」に見えないようにする

「遠目の木の山、もっと密接するレベルで近づけて、密度を上げて欲しい。スマホならいい感じ
なんだけど、iPadやパソコンだと広すぎて、謎の山になって違和感がある」から。

- 塊は 幅58m・高さ26m・70m間隔 だった。2km先で画面の高さ1,000pxに対し約22pxあり、
  大きな画面では1つ1つが離れた小山として読めた。
- **幅36m・高さ18m・32m間隔**（隣と触れ合うくらい）にした。でこぼこした樹冠の面として
  手前の木に続く。明るさは 0.50〜0.82 → 0.40〜0.66 倍。
- 数は約5倍（いちばん濃い森の上で、見せる塊 11,167→53,493個）。面を7→5枚にして
  三角形を約27万枚に抑え、縮めない塊の行列は一覧を作るときに組み立てておいて、
  詰め直しでは写すだけにした（1回 6.6〜8.7ms）。一覧の作り直しは72フレームに分けて
  1フレーム1.3〜1.9ms。

---

## 00.013.004.001 — 飛行中のアイドル、離陸で出力を落とす、ホバリングの高さ、2本の空港で草地に降りるのを直す

「飛行中に全てのエンジンの出力が0%になることはないようにして」「自動操縦で離陸する時、
一瞬エンジンの出力を弱める」「ホバリング中にホバリングする高度をいじれるように」
「複数滑走路のところだと、特に2本のところだと、ど真ん中の草地に着陸しちゃう」から。

- **フライトアイドル。** 空中では、はしごのいちばん下のグループを「最大の5%」か
  「重さの3%」の小さいほうより下げない。以前は1回の飛行で6〜138秒、全エンジンが0%だった → 0秒。
  垂直離陸用エンジンが回っているあいだは前のエンジンを0まで絞れる。計器は「アイドル」と出す。
- **離陸で出力を落とさない。** 浮いたあとの頭打ちを「上昇の速度」から、上昇の段と同じ
  「曲がれる速さの1.5倍」にした。浮いた1秒以内に三式戦闘機・TB2が0%、Concordeが67%まで
  落ちてから全開へ戻していた → 浮いてから15秒間100%。
- **ホバリングの高さ。** ホバリング中は Shift／Ctrl かタッチの ▲▼ で保つ高さを毎秒2.5m 動かせる。
  目標高度のスライダーでも決められる。対地3mより下へは下げない。
- **2本の空港で草地に降りる。** 「実際に使う滑走路の中心」を定義の向きのまま測っていたので、
  UIで向きを変えた空港では240m横の草地を狙っていた。いまの向きで測る。
  平行滑走路の空港で滑走路を外した進入 56/100回 → 0回。離陸位置（R）も同じく直った。
- 6機の全自動飛行はどれも着陸、やり直し0回のまま（接地は少し穏やかに：747 -206→-54fpm、
  Concorde 6.0→4.5G）。TB1の滑走中のG（12.2→19.1G）は接地ではなく、260〜60ktでの
  滑走中に前脚で機首が跳ねるもので、飛ぶ経路が変わったぶん大きさが変わった（もとからの癖）。
- 検証を足した：`verify-flight.js` (13)（浮いた直後に絞らない・空中で全エンジン0%にならない・
  アイドルは重さの3%まで）、`verify-world.js`（向きを変えた平行滑走路の空港で中心が滑走路の上）。

---

## 00.013.004.000 — 道路が地面に潜る・角で細る・回した空港に合わない、誘導路のずれを直す

アーカイブ：**`19f26bc`**（節 00.013.004 の最後）

「街灯の頻度を3倍に」「道路も明るく（黄色っぽいうっすいブルーム）」「空港が回ってると道路と
ターミナルが合わない」「角で細くなる」「わずかな起伏で道路が埋もれる」
「一部の空港で誘導路と滑走路がずれてる（向きを初期値から変えると起こる）」から。

### 道路が地面に潜っていた

道の頂点を400〜900mおきに置いていたので、あいだにある地形の折れ目（格子は312m）を道が
弦で素通りしていた。**路面の約3割が地面より下**にあった。
道の中心線と左右の縁が地形の格子線を横切る位置すべてに頂点を置き、縁ごとに高さを取るようにした。

| 起伏の大きい街のまわり | 地面より下 | 最深 |
|---|---|---|
| 直す前 | 30.5〜33.8% | 157m |
| 直した後 | 0.35〜0.72% | 6.5m |

高さを取るLODも、カメラからの距離ではなく**タイルが実際に使っている分割数**
（`terrainLodAt`）にした。頂点が増えたぶん（510→9,594）、格子点の高さを覚えておき、
作り直しは1フレーム4msずつ順番に進める。

### 角で細っていた

幅を前後の区間の平均の向きに振っていたので、角で cos(曲がり角/2) 倍に細っていた。
逆数を掛けて保つ（上限2.5倍）。縁どうしの最短距離は最小25.1m → 26.0m。

### 回した空港に道が合わなかった

UIで滑走路の向きを変えるとターミナルも回るが、道は定義の向きの車寄せへ着いたままだった
（+60°で1.3〜1.8km、+135°で2.5〜3.3kmずれ）。向きを変えた空港の支線は、空港のまわりを
円弧で回り込んでから今の向きの車寄せへ入れる。ずれは0m、舗装に乗る頂点も0。

### 誘導路が滑走路に着いていなかった

誘導路はターミナルの正面から1本だけ出していたが、ターミナルは定義の最大長から位置が
決まるので、**定義どおりの長さでも滑走路の端より30m外**で分かれていた（縮めると最大1,530m外）。
滑走路と平行な誘導路を全長に通し、両端と中央の3か所で滑走路へ出るようにした。
出口702本すべてが滑走路の上。滑走路を縮めたときに草地からはみ出すターミナルの下も広げた。

### 街灯を3倍に、にじみと夜の路面

「260m間隔」のつもりが経路の頂点の上にしか置けず、実際は平均916mおきだった。
線に沿って300mおきに置く（3倍の密度）。黄色がかった薄いにじみ（3.4倍・0.16）を重ね、
夜は路面にも暖かい色をほんのり足す。

---

## 00.013.003.000 — 消えた都市と空港を戻し、一覧を固定データにする

アーカイブ：**`215902f`**（節 00.013.003 の最後）

「もともとあったはずの都市や空港がいくつか消えてる（イーゼンハイムや、NOISとか）」
という指摘から。**私が入れた劣化で、指摘のとおりだった。**

### 何が起きていたか

山脈の作り替え（00.012）で地形が変わり、都市・空港の「置ける場所」の判定結果が変わった。
ところが名前は**置けた順に乱数列から引いていた**ので、1つ判定がずれると
**それ以降の地名が全部ずれた**。

| | 山脈を直す前 | 直した直後 |
|---|---|---|
| 都市 | 130 | 130（**うち38件が別の名前に入れ替わり**） |
| 空港 | 78 | 72（**20件消えて14件新設**） |

消えた38都市の座標を新しい地形で判定し直すと、**29件は今でも問題なく置ける**。
つまり地形のせいではなく、名前がずれただけだった。
残る9件（イーゼンハイムは起伏833m・上限420m、クロノは1,249m）は本当に山の中になっていた。

### 直し方

都市と空港の一覧を**固定データ**（`WORLD_CITY_DATA` / `WORLD_AIRPORT_DATA`）にした。
地図は覚えて使うものなので、地形をいじるたびに地名が動くべきではない。

- 表は山脈を直す前の世界のものをそのまま持ってきて、新しい地形で1件ずつ検証した。
- 置けなくなったものは**名前を保ったまま近くの平地へずらした**。
  - 都市：115件そのまま／15件移動（イーゼンハイムは4km、最大はパルリスの32km）
  - 空港：65件そのまま／13件移動（最大はORKUの42km）
  - **落としたものは0件。130都市・78空港がすべて戻った。**
- 派生する値（市街地の半径・街路の向き・滑走路の本数など）は今までどおり計算する。
- **標高は地形から取り直す。** 座標は固定だが高さは固定しない——固定すると、
  地形を変えたときに空港だけ台地や窪地になる。

### 検証の誤りも直した

道路の検証に「1本ずつ、直線より坂が緩いこと」を入れていたが、**これは成り立たない**。
A* が最小にしているのは「距離 + 26×高低差 + 水と空港の割増」の合計であって、
最大傾斜でも登りの合計でもない。世界が変わったら最大傾斜で3本、登りの合計に変えても
13本が落ちたが、中身を見るとどれも正しい選択だった
（road-vestaria-marlton-vestaria-greenis は最大傾斜 0.46→0.65 だが、
登りの合計は 4,869m→4,372m と減っている）。
1本ずつの要求をやめ、**全体の分布**（最大傾斜の9割点が直線の8割未満）で見るようにした。

### いまの世界

都市130 / 空港78（滑走路3本＝4・2本＝6・1本＝68）/ 川68 / 湖49 / 山脈10 / 峰46 /
道路196本（幹線118＋支線78、総延長14,435km）。
滑走路を横切る道0本・舗装にかかる道0本・車寄せに着く支線78/78。

---

## 00.013.002.000 — 空港にターミナルと複数滑走路、道路を空港と結び直し、遠くの森を木に戻す

アーカイブ：**`a40795d`**（節 00.013.002 の最後）

「滑走路のど真ん中を道路が横切る」「空港への道が滑走路の真ん中に着く」
「遠くの木の塊が山に見える」という指摘から。

### 道路と空港のつなぎ方

| 測ったもの | 直す前 | いま |
|---|---|---|
| 滑走路そのものを横切る道 | **72本** | **0本** |
| 空港の舗装（滑走路・誘導路・エプロン）にかかる道 | 79本 | **0本** |
| 支線の終点 | 全部が空港の中心＝滑走路の真ん中（距離0m） | 全部がターミナルの車寄せ |

- 支線の終点を **ターミナルの車寄せ** にした。
- 空港の舗装を経路探索で**通行止め**扱い（距離の倍率400倍。水の5倍よりずっと重い）。
- 車寄せへ直接A*を走らせても最後の詰めが利かない（格子の目が3.5kmあるので、
  舗装の上を通ってから着く道が32本残った）。**陸側に取り付き点を置き、
  そこから車寄せまでは直線**で入れる。ターミナルの正面なので滑走路をまたがない。
- それでも2本が滑走路にかかった。Chaikinで角を落とすと経路が内側へ寄るため。
  **経路探索のときだけ空港の判定を700m広げる**ようにして0本になった。

### ターミナルと複数滑走路

- 首府の空港は滑走路が複数本になった（3本＝5空港、2本＝5空港、1本＝62空港）。
  間隔480mの平行滑走路で、数字には実際の空港と同じ **L / C / R** を付ける。
- ターミナルはエプロンに面した建物＋搭乗橋（ピア）。規模は滑走路の本数で決める
  （1本＝間口110mの小屋、3本＝340m・ピア3本）。
- 位置は**世界の定義から取る**。滑走路の長さはUIで変えられるので、そこから割り出すと
  ターミナルが動いて、そこへ向かって引いてある道路とずれる。

### 道路の街灯

260m間隔。建物は置かず、街の灯りと同じ加算合成の点だけ。明るさは街明かりに合わせた
（街の灯りが1.0のとき道路灯は0.75）。灯りは小さな点なので、道の帯（55km）より手前
（26km）で切る——それより遠いと点が潰れて線にならず、頂点だけ増える。

### 遠くの森を「木の塊」に戻す

以前は 幅105m × 高さ30m を 90m 間隔に置いていた。**幅が間隔より広いので必ず隣と重なり**、
25%地面に埋めてあったのと合わせて、ひと続きのなめらかな面に溶けていた。縦横比も3.5:1と
平たいので、森ではなく小山の連なりに見えた。色も、塊は幅の広いなめらかな面なので光を
よく受け、同じ色を与えると手前の細い木より明るく出る——手前は濃い緑の木立なのに
その先だけ淡い緑になって、木が終わって草地の丘が始まったように見えていた。

いまは 幅58m × 高さ26m を 70m 間隔（隣とのあいだに地面が見える）、縦横比2.2:1、
埋め込み10%、明るさ 0.50〜0.82倍（以前は 0.74〜0.98倍）。

### 直しながら見つけて直したもの

- **平行滑走路を足したら、離陸も着陸も草の上になるところだった。** 空港の中心
  (a.x, a.z) は滑走路と滑走路のあいだの草地なのに、離陸位置も自動操縦の進入も
  そこを狙っていた。「実際に使う滑走路の中心」を見るようにした。
- **方位を変えると滑走路の数字が1本も描き直されなくなるところだった。**
  数字が滑走路ごとのサブグループの中へ移ったので、グループ直下を探す実装では
  見つからない。本数ぶん入れ替えるようにした。

### 検証に足したもの（tools/verify-world.js）

滑走路の平坦チェックを、**全滑走路とターミナル・車寄せまで**見るように広げた
（中央の1本だけ見ていると、外側の滑走路が斜面に乗っていても気付かない）。

---

## 00.013.001.000 — 山に名前と標高を付ける

アーカイブ：**`2419dcb`**（節 00.013.001 の最後）

山脈には名前があったが、個々の山には無かった。稜線を歩いて峰を拾い、46座に名前と
標高を付けて、頂のすぐ上に `Mt. Aquilanum  4,791m` と出す。
飛んでいるとき「この尾根はいくつか」がすぐ要るので、標高を併記した。

| 山 | 標高 | 突出度 | 山脈 |
|---|---|---|---|
| Mt. Aquilanum | 4,791 m | 4,338 m | Astra Massif |
| Mt. Praeturn | 4,645 m | 888 m | Astra Massif |
| Mt. Tarnurn | 4,515 m | 581 m | Astra Massif |
| Mt. Stensten | 4,044 m | 748 m | Nordheim Range |
| Mt. Karholm | 3,991 m | 3,995 m | Nordheim Range |

- 選ぶ基準は高さではなく**突出度**（より高い峰へ向かう途中でいちばん下がるところまでの落差）。
  単に高い順だと、大きな山の肩が2位3位を占めて名前が固まる。
- 候補ごとに**山登りで本当の頂へ寄せる**（粗い格子だと尾根の肩を拾う）。

### 直しながら見つけて直したもの

- **世界の最高地点に名前が付かなかった。** 稜線の真上だけを見ていたため。
  最高地点は稜線から5.2km外れていた。稜線に直交する向きへも振るようにした。
- **横へ振る幅を短軸半径に対する割合にしたら、まだ取り逃がした。** アストラ大山脈の
  短軸半径は210kmもあるので、0.45の割合で9点に振ると刻みが23.6kmになり、
  5.2km外れた頂をまたいで素通りする。絶対値（±12kmを1.5km刻み）に変えた。
- **`WORLD_MAX_TERRAIN_M` の根拠にしていた実測値が過小評価だった。**
  格子を粗く切って探した4,708.8mに対し、峰を拾う仕組みで探すと **4,791.8m**。
  格子の目が峰をまたいでいた。閾値5000mはそのままで足りるが、README と
  コメントの数字を直し、今後は峰の仕組みで測ることにした。

### 検証に足したもの（tools/verify-world.js）

峰の名前が一意か・**札の標高が実際の地形と一致するか**（数字が嘘だと意味がない）・
峰どうしが20km以上離れているか・最高峰が `WORLD_MAX_TERRAIN_M` を超えないか。

---

## 00.013.000.000 — 街と街・街と空港を道路で結び、街に街路を通す

アーカイブ：**`52e8cf9`**（節 00.013.000 の最後）

世界に道が1本も無かった。街は箱がばらまかれているだけで、区画も通りも無かった。

### 道路（幹線119本＋空港への支線72本、総延長 13,942km）

直線で結ぶと山も川も突っ切る。実際の道路は谷を選んで登り坂を避けるので、
**傾斜と水にコストを付けたA\*** で引いた。

| 測ったもの | 探索した道 | 同じ端点を直線で結んだ場合 |
|---|---|---|
| 最大傾斜の9割点 | **0.28** | 0.43 |
| 登りの合計（中央値） | 323 m | 394 m |
| 総延長 | 13,942 km | 13,227 km（＝遠回り 5%） |

- 国内の街は**最小全域木**で結んでから、木の上で遠回りになる組に近道を足して環を作る。
  木のままだと必ず行き止まりの枝分かれになって、上空から「網」に見えない。
- 経路探索は世界ぜんぶではなく、**2点を結ぶ直線のまわりの帯（回廊）**だけを格子にする。
- 127/130 の都市がつながる（残り3つは島なので道は通らない）。
- 地形は刻まない。幅26mに対していちばん細かいLODでも地形の頂点間隔が312mあるので、
  刻んでも再現できない。滑走路の路面標識と同じくデカールとして地形の上に重ねる。

### 街路（碁盤の目）

- 向き（0〜90°）と街区の大きさ（150〜230m）は街ごとに変える。全部同じだと、
  上空から見て世界中の街が同じ判子に見える。
- 建物は街路の上に来たものを**弾くのではなく、街区の内側へ押し込む**。
  弾く作りだと間口（最大37m）ぶんの余白まで要って街区の半分近くが使えなくなり、
  1都市あたりの建物が230軒から124軒まで減ってしまう。
- 街区に収まるようになったぶん軒数を増やした（167〜520軒 → 350〜1,340軒）。
- 街路の位置は世界側の `worldCityStreetDist` が決める。**建物を建てない判定と
  舗装を描く位置が同じ式を見る**ので、建物が道を塞ぐことがない。

### 直しながら見つけて直したもの

- **海の上を走る道ができた。** 全域木は陸か海かを見ずに街を結ぶので、島がいくつもある国
  （セラフィナ諸島など）で 156km・117km・111km を海の上で渡る道が引かれていた。
  連続して海の上にいる距離が2.5kmを超えたらその道は捨てる。川を渡る橋
  （世界ぜんぶで18km）は残した。
- **3.5kmの格子では幅300mの川が見えず、避けるどころか増えていた。**
  水の上を通る距離の9割点が、直線の501mに対し探索した道は878m。格子点のまわりも見て、
  川を格子1つぶんの太さの障害物として扱うようにした。
- **碁盤の目が一方向の縞にしか見えなかった。** u方向とv方向では「進む向き×幅の向き」の
  手前・奥が逆になるので、同じ巻き順で三角形を張ると片方が裏を向いて背面カリングで消える。
- **街路の向きを決める乱数で、街の位置がぜんぶずれた。** 配置用の `rand()` を街路の
  ぶんだけ余計に消費したため。ステンハイムが標高差270mの斜面に乗って検証が落ちた。
  街路の向き・街区の大きさは**街の名前から引いた別の乱数**で決めるようにした。
- **街に近づくたびに引っかかるようになった。** 1都市の生成が30〜42msかかる
  （建物3.7万頂点＋街路5,700頂点）のに、圏内に入った街をその場で全部建てていた。
  地形タイルと同じくキューに積んで、1フレームに1つずつ建てる。
- **街の輪郭がコンパスで描いた円になった。** 街路を円で切っていたため。
  線ごとに長さをばらつかせて外周をぎざぎざにした。
- 山脈の変更で空港が 78→**72** に減っていたのを、前回 75 と書いていた（測った時点が
  三角州の修正より前だった）。README と前回のCHANGELOGを直した。

### 検証に足したもの（tools/verify-world.js）

道路が海を渡っていないか・直線より坂が緩いか・大半の都市が道路でつながっているか。
上の「海の上を走る道」はこの追加で見つかった。

### 残っていること

幹線は街の**中心**で終わるので、街路の上を重なって走る。街の外周で街路網に
接続するようにしたほうが自然。

---

## 00.012.000.000 — 山脈に稜線を持たせて「連なり」にする

アーカイブ：**`9c750c2`**（柱 00.012「山脈の稜線」の最後）

山脈が丸い塊にしか見えなかったのを直した。原因は作り方そのもので、
「回転楕円の中心からの距離で減衰させたドーム」にリッジノイズを掛けていた。

| 測ったもの | 直す前 | いま |
|---|---|---|
| 最高点の6割以上の場所が割れている数 | **29〜108個** | — |
| 稜線を端から端まで歩いたときの峠（5割割れ） | — | **2〜5ヶ所** |
| 稜線上の峰（最高点の85%超） | — | **6〜18座** |
| 高い場所の主軸比（細長さ） | 1.5〜2.2 | — |
| 陸のうち標高1000m超 | 10.6% | 20.4% |
| 陸のうち標高3000m超 | 0.1% | 1.7% |
| 世界の最高地点 | 4,382.9m | **4,708.8m** |
| 標高2500m超の区間の幅（中央値） | 750m | 1,200m（最長207km） |

上空22kmから撮ると、稜線のない雪原にでこぼこが散らばっているだけで、
山脈の背骨がどこにも無かった。画面写真はLODと霞で潰れて形が判らないので、
`worldHeightAt` を直接描く陰影起伏図を作って確かめた。

### 稜線を1本引く

- **長軸方向は中ほどを平らに保ち、両端だけ落とす。** 楕円の減衰をそのまま使うと
  `mask*mask` が長軸の半分で0.36まで落ちて、中心だけが高いドームになっていた。
- **短軸方向は稜線からの距離で落とす。** 稜線は正弦2本で蛇行させ、まっすぐな棒に見せない。
- **稜線の芯（短軸半径の30%）だけを下支えする。** 峰は動かないので、峰の高さと
  山腹の起伏はそのままに、連なりだけが切れなくなる。
- **片側だけ急にする**（1.25〜1.80倍）。実際の山脈の非対称に合わせたもので、
  どちら側が急かは山脈名から決める。

### 直しながら見つけて直したもの

- **下支えを山脈ぜんぶに掛けたら、稜線ではなく広い高原になった。**
  最大傾斜が 14〜43% から 10〜28% に**下がり**、標高1000m超の陸が 26.2% まで増えた。
  下支えを稜線の芯だけに絞り、さらに「峰は持ち上げない」式に変えて直した。
- **稜線が碁盤の目のように直角に折れていた。** 値ノイズは格子点の双一次補間なので、
  リッジノイズの稜線（`|2n-1|`が0になる線）が格子の向きに揃ってしまう。
  サンプル位置を別のノイズでずらす（ドメインワープ）ことで、
  曲がった尾根と、そこから枝分かれする支尾根が出るようになった。
  `worldHeightAt` は山脈の中で 0.818 → 0.957 μs/回（+17%）。
- **`WORLD_MAX_TERRAIN_M` が実際の最高地点より低くなった。** 広い当たり判定の足切りに
  使っていて、低いと高い山を素通りする。10本の山脈を240×240で探して山登りで詰め、
  4,708.8m（アストラ大山脈 x=-836305 z=-233853）を実測して 4600 → 5000 に上げた。
- **三角州の中州が1つ沈んだ。** 海岸線が動いてターンオル川の分流が2.6kmまで短くなり、
  分流の川床(-12m)から谷の斜面(0.03)で水面に戻るのに400m要るのに、
  分流の間隔が546mしかなかった。分流を本流の半分の深さにして（実際の三角州も
  分流は本流より浅い）、堆積も扇の内側45%までは減らさないようにした。
- 空港が 78→72、湖が 50→49 に減った。山が増えて平地が減ったぶん。
  全10ヶ国に都市と空港がある条件は満たしている。

### 雪を傾斜でも決めるようにした

標高だけで塗ると、雪線から420m上はぜんぶ同じ白になり、せっかく引いた稜線も谷も
画面上で消える。急な面には雪が積もらないので岩を出す。しきい値は下の
「急斜面は岩肌」(0.32)より緩い0.14にした——遠景ほど地形メッシュの法線が均されて
傾斜が小さく出るため。雪線より上で傾斜0.32を超えるのは頂点間隔312mで38%・1250mで15%
しかないが、0.14なら78%・59%ある。

### 検証に足したもの（tools/verify-world.js）

稜線を端から端まで歩いて、峠が多すぎないか（＝ちぎれた山の列になっていないか）・
峰がいくつもあるか・世界の最高地点が `WORLD_MAX_TERRAIN_M` を超えていないか。

### 残っていること

**雪山が画面上で白飛びして、地形の形が見えない。** 真上16kmから撮って、
描かれているメッシュと `worldHeightAt` の差が平均21m・最大138m（＝地形は正しく
描かれている）ことを確かめたうえで、それでも一面の白に見える。稜線の網目が
うっすら判る程度。霞ではない（高度16,000mでは霧の減衰はほぼ0）。
雪の色・全体の露出・ブルームのどれかの問題で、山脈の作り方とは別の話。次の課題。

---

## 00.011.000.000 — 川の河口を入り江・三角州にし、川幅を流量で決める

アーカイブ：**`87e3d25`**（柱 00.011「川の河口」の最後）

「川の河口が海岸でぶつ切りになっている」ところから。まず現状を測った。

| 見たもの | 実測（直す前） |
|---|---|
| 川の本数 | 68本（海へ57・湖へ11） |
| 長さ | 最短31km / 中央81km / 最長139km |
| **河口の半幅** | **最小107m / 中央107m / 最大107m** |
| 蛇行度（実長÷直線） | 中央1.29（実際の川は1.3〜3なので悪くない） |
| 区間ごとの曲がり角 | 中央 0.0°（半分の区間が完全な直線） |
| 4km以内まで近づく川の組 | 2,278組中 4組 |

### 1. 川幅が流量を見ていなかった

半幅の式が `12 + (源流からの割合) × 95` で、**長さに関係なく「何割来たか」だけ**で
決まっていた。だから 31km の川も 139km の川も河口の半幅が 107m ちょうどで揃い、
空から見て大河と小川の区別がつかなかった。

集水面積は流路長のおよそ1.8乗、川幅は流量のおよそ0.5乗で増える。合わせると流路長の
0.9乗になるので、少なめに見積もる側の平方根で近似して
**半幅 = 0.30 × √(源流からの距離)** にした。河口の半幅は **91〜268m** に散った。

### 2. 河口が海岸でぶつ切りだった

海岸の傾きを見て、入り江か三角州で海につなぐ。河口から沖へ12kmの海底を
上流の向き ±90° の9方向で測り、いちばん深い向きを沖とする。

| 沖12kmの深さ | 川の長さ | 形 | 本数 |
|---|---|---|---|
| −45m より浅い | 65km 以上 | **三角州**（土砂が溜まって流れが分かれる） | 6 |
| それ以外 | | **入り江**（谷がそのまま海に沈む＝溺れ谷） | 50 |

どちらも河口の川床を海面下12mまで下げる。地形が海面(y=0)より低くなればそこは
自動的に海になるので、「海が内陸へ入り込む」形が地形だけで出る。
セヴェルエント川の河口は実測で **幅1,900m・内陸へ900m** の入り江になった。

三角州は「積もらせてから切り開く」。先に扇形の堆積で海面より +4m の中州を作り、
そのあと分流3本を刻む。順番が逆だと海底を掘るだけで、水面下で何も見えない。

### 直しながら見つけて直したもの

- **河口の街が水没した。** 川の刻み込みは街の均しより後に効くので、掘ったぶんは
  街にもそのまま効く。グリムフィヨルド川は街の中心から98mのところを流れていて、
  イーゼンダールが標高 −0.3m まで沈んだ（verify-world が検出）。
  河口の掘り下げの強さを**川ごとに1つの値**にして、街の均し範囲にかかる川では弱めた。
  点ごとに弱めると、街の手前だけ川床が持ち上がってそこに水が溜まる。
- **川床が下りきらず途中で上がった（2本）。** すでに海面下12mより深い川床を持つ川
  （急な海岸に出る川）で、−12mへ寄せる処理が川床を**持ち上げて**いた。下げるだけにした。
- **三角州の中州が沈んだ（6本中2本）。** 分流の向きをただの乱歩にしていたため6歩で
  ±0.7rad も流れ、分流どうしが交わって中州が海面下2.5mまで削られていた。
  前の値を0.85倍して引き戻し、河口からの距離に比例した上限（最大±0.30rad）で頭打ちに。
  弦からの横ずれは中央325m、隣り合う分流の隙間は最小944m で交わらない。
- **分流が陸のなかで行き止まりになった。** 三角州になる海岸はとても遠浅で、河口から
  5kmでもまだ地面が標高+1.5mある。長さを決め打ちにすると水路がただの池になるので、
  素の地形が海面下に落ちるまで歩かせるようにした（上限12km）。
- **分流を沖ほど深く掘ると中州まで削れた。** 谷の勾配が0.03なので10m深くするだけで
  333m 余分に削れる。分流の深さは河口のまま一定にした。
- **河口の水面が二重になった。** 川床が海面下なのに水面の帯を張っていたので、
  半透明の面が海と重なって河口だけ帯状に暗くなっていた。帯は海面より上のあいだだけ張る。

### 検証に足したもの（tools/verify-world.js）

幅が下流へ広がるか・川床が河口の掘り下げ込みで下り一方か・河口に沈んだ街がないか・
河口の幅が川ごとに違うか（最大が最小の2倍以上）・三角州に中州と分流の両方があるか。
上の不具合はどれもこの追加で見つかった。

**次の川の作業**：蛇行（区間ごとの曲がり角の中央値が0.0°のまま）と支流（合流）。

---

## 00.010.000.000 — 森のある所と無い所の色の段差を無くし、遠くにも森を出す

アーカイブ：**`fd69555`**（柱 00.010「森の色と遠景」の最後）

「木が生えている所と生えていない所で色が違って見える」「遠くの森がのっぺりしている」
という指摘から。原因は2つあり、どちらも直した。

### 1. 地表の色と樹木が、別々の式で森を決めていた

木が生えるのは手前1.2kmまでで、その先は地表の色だけになる。ところが
地表色（03c-terrain.js）と樹木（03g-vegetation.js）が別の式を持っていて、
陸を20,000点サンプルすると **|地表色の森混合率 − 木の密度| が平均0.45** もあった。
しかも食い違いは両方向：

| 場所 | 地表色の森混合率 | 木の密度 |
|---|---|---|
| 標高397m・乾燥0.63 | 1.00（森の色） | 0.00（1本も生えない） |
| 標高883m・乾燥0.58 | 0.79 | 0.00 |
| 標高40m・温暖湿潤 | 0.00（草地の色） | 0.50（生える） |
| 標高298m・温帯 | 0.00 | 0.59 |

下2行が指摘の症状そのもので、手前だけ濃い緑の木が立ち、その先に明るい草地の色が
のっぺり続くため、**木の出る半径が地面の色の段差として見えていた**。

- 式を `worldForestDensity`（03b-world.js）1つに統一。地表色・1本ずつの木・
  木立の塊の3つがこれを見る。統一後の食い違いは **0.000000**。
- 森林限界の上側も、森の色ではなく低地の色から始めるよう直した（密度は森林限界で
  0へ落ちきるので、そこから森の色で再開すると段差になる）。
- 地表色に **波長400mの斑** を足した（濃い森ほど強い）。もともとの明暗は波長2.4kmで、
  地方ごとの色ムラとしては効くが、木立の粗密としては大きすぎた。
- `verify-world.js` に森の検証を追加：森の面積割合、森林限界での連続性
  （4mごとの変化が0.06未満）、砂漠・極寒・市街地・森林限界より上・切り立った斜面で
  0になること。

### 2. 1.2kmの先に何も無かった → 木立の塊を4.5kmまで置く

1本ずつの木のさらに外側に、**木立の塊**（低ポリの丸い山・幅105m・高さ30m）を撒く層を
足した。これで森は3段構え（〜1.2km 木／〜4.5km 塊／それ以遠 地表の色と斑）になり、
3段とも同じ濃さと同じ色を見るので切り替わりが分からない。

- **板（ビルボード）ではなく塊にした。** 飛行機からは森を真上に近い角度で見ることが
  多く、縦の板は真上から見ると線になって消える。塊なら上からも横からも形が残り、
  不透明なので重ね合わせの順番も塗りつぶしの重さも要らない。
- **タイルに分けず、まとめて1つの `InstancedMesh`。** タイルで覆うと描画呼び出しが
  200を超える（画面全体で今183）。ひとかたまりに持ち、カメラが動いたら見せる範囲のぶんを
  配列の先頭へ詰め直して `mesh.count` で切る。
- ふちでは塊を小さく潰して地表の色へ溶かす。これが無いと、直したかった円い境目が
  遠くへ移動するだけになる。
- **木が消える高さ（対地2,500m超）では、塊が手前を空けるのをやめる。** 空けたままだと
  真下に半径1.2kmの木の無い円ができ、上空からくり抜かれて見える。
- 高さは `terrainSurfaceHeightAt`（高さ関数4回）ではなく `worldHeightAt`（1回）で取る。
  候補が1万個近いので差がそのまま走査の重さになる。塊は1.2kmより遠くにしか出さないので
  両者の差は見えない（念のため高さの25%を地面へ埋めてある）。
- 実測（濃い森の上・900×520）：**三角形 +41,881（5,983個×7面）／描画呼び出し +1**。
  一覧の作り直しは13フレームに分散して合計34ms・最悪のフレーム3.4ms、詰め直しは1回0.06ms。

verify-world.js（森の検証を追加）/ verify-flight.js / fleet2.js すべて通過。

---

## 00.009.000.001 — 環境画面のメニュー開閉ボタンが狭い画面で隠れるのを直す

アーカイブ：**`225dbcf`**（柱 00.009「パフォーマンス設定」の最後）

縦画面のスマホ（幅320〜375px）で開くと、右メニューの開閉ボタン（`#envBtnPanel`）が
画面の外に押し出されて**見えも押せもしなかった**。実測（Playwright）：375px幅で
ボタンのx座標が450px（画面は375pxしか無い）——ブランド名「FLIGHT SIM ENVIRONMENT」
と「← 機体設定（Builder）に戻る」の合計幅が入りきらず、`overflow:hidden`の
トップバーからボタンごとはみ出していた。しかも同じ幅では右メニューが常に
画面下40%を占めるレイアウトに切り替わっている（`@media (max-width:760px)`）ので、
**開閉ボタンを押せない＝メニューを畳んで3D側を広げる手段が無い**、という組み合わせの悪い
状態になっていた。

### 直し方：縮んでいい要素と、縮んではいけない要素を分ける

`#envBtnPanel`に`flex-shrink:0`を付けて**絶対に縮まない**ようにし、代わりに
ブランドの副題（「ENVIRONMENT」）・区切り線・戻るリンクの説明文を、狭い画面では
順に削っていく。

- 600px以下：副題「ENVIRONMENT」と区切り線を消し、戻るリンクの文言を
  「← 機体設定（Builder）に戻る」→「← Builder」に短縮
- 360px以下：戻るリンクそのものを消す（開閉ボタンより優先度が低いため）

実測で280px〜900pxの16段階すべてにおいて、開閉ボタンが画面内に収まり
（横スクロールも発生せず）、かつボタンの中心座標をクリックしたときに実際に
ボタン自身がヒットする（他の要素に隠れていない）ことを確認した。

### ついでに、右メニューの余白も詰めた

同じ`max-width:760px`のブレークポイント（右メニューが下段の40vhに変わる境目）で、
セクション見出し・入力欄・ボタンの上下の余白を1〜3px詰めた。文字サイズは
これ以上落とすと読みにくくなるのでほぼ変えていない——狭い帯の中でのスクロール量を
減らすのが目的で、可読性を犠牲にする話ではないため。

verify-world.js すべて通過（このコミットはHTML/CSSのみで、飛行物理・世界生成には
触れていない）。

---

## 00.009.000.000 — パフォーマンス設定（画質プリセット）を足す

弱い端末でもコマ落ちしにくいように、地形・植生・街・雲の「どこまで実体化するか」と
解像度をまとめて3段階（高／中／低）で切り替えられるようにした（右パネルの
「パフォーマンス」）。既定は「高」＝これまでの固定値そのままなので、何も触らなければ
誰にも影響しない。

### 触るのは「範囲」と「解像度」だけ

地形の近くの分割数（`TERRAIN_LOD_STEPS[0]`＝96分割・約310m間隔）はどの画質でも
変えない。`terrainSurfaceHeightAt()`（木・建物の設置、脚の接地、モデル形状の
当たり判定がみんな使う「描かれている地面の高さ」）は常にこの分割数で地形メッシュを
補間する前提で作られているので、ここを画質で変えると見えている地面の分割と
当たり判定の格子が食い違い、脚や当たり判定点が地面から浮いたりめり込んだりする。
なので画質が触るのは、実体化する**範囲**（地形300km→中180km→低120km、樹木・街・
雲の最大クラスタ数も同じ倍率）と**解像度**（devicePixelRatioの上限）だけにした。

範囲の倍率をそのまま掛けると小さすぎる値になる場合があるので、系統ごとに
最低ラインを敷いた（低画質でも地形はタイル4枚ぶん、樹木はタイル1枚ぶん残す）。

### 雲だけは「作り直し」が要る

樹木・街・地形はカメラが動くたびに範囲外を自然に捨てる作りだが、雲はクラスタ
（スプライトの塊）を起動時に最大数ぶんあらかじめ作っておき、雲量スライダーでは
表示/非表示を切り替えるだけ（ジオメトリを毎回作り直さない設計）。画質でクラスタの
最大数そのものを変えるときだけは、いったん破棄して`initClouds()`をやり直す
（`rebuildClouds()`）。スプライトのテクスチャは画質に関係なく同じものを使い回す
ようにした（`initClouds()`を素直に呼び直すだけだとテクスチャも毎回焼き直して
古いものが解放されず溜まっていた）。

### そのほか

- 画質を切り替えると、地形・植生・街・雲をその場で作り直す（次にカメラが動くのを
  待たない）。起動時に保存済みの画質を読み込んだときも同じ経路で1回適用される。
- 実際のコマ数を「パフォーマンス」パネルに0.5秒おきで出す。設定を変えた効果が
  その場で見えるようにした——効果を謳うだけで確かめる手立てが無い設定は意味が薄い。
- Playwrightで、画質を切り替えると実体化半径・雲のクラスタ数・解像度上限が実際に
  変わること、保存→読込で画質が復元されることを確認した。

verify-world.js / verify-flight.js すべて通過。

---

## 00.008.000.000 — モデル形状からの当たり判定を足す

アーカイブ：**`24cda83`**（柱 00.008「モデル形状からの当たり判定」の最後）

いままでの接地判定は、Builderで置いた脚の点しか地面と当たらなかった。山に
まっすぐ突っ込んでも、脚を出し忘れて胴体着陸しても、翼端を擦ってもすり抜けて
いた。読み込んだGLB（内蔵機なら組み立てメッシュ）の頂点そのものから、当たり
判定点を作るようにした。

### 頂点の塊を26方向の外側極値だけに落とす

全頂点を毎フレーム調べるのは重いので、立方体の6面＋12辺＋8頂点の向き（26方向）
それぞれの**いちばん外側の頂点**だけを残す（k-DOPという手法の簡易版）。凸包
そのものではないが、翼端・機首・尾部・ポッドのような出っ張りはたいてい
どれかの方向の最外点として拾える。

### 脚を除かないと、着陸のたびに「機体が地面に当たった」ことになる

脚はそこに接地する設計なので、脚の頂点をそのまま当たり判定に含めると、脚の
ばね物理と二重に判定してしまう。重心から脚の接地点までの深さ（`gearHeight`）
を基準に、重心に近い側35%を「脚」とみなして除く（固定距離ではなく比率に
したのは、機体の大小で脚の長さが1桁以上違うため）。実測（サンダーバード1号、
13点）：除いたあと脚の接地点より下に残った点は0。

### 「重心の真下の地面」だけでは判定できない

機首の先や翼端は重心と別の(x,z)にあり、そこに崖や山があるかもしれない。
26点は互いに離れた場所を指しているので、重心の真下だけを見て済ませると、
崖にかすめて突っ込む場面を見逃す。世界のどこにもこれより高い地面が無いという
**絶対高度**でしか安全に判定を省けない。実測で世界最高峰（山脈定義
height:4400の「アストラ大山脈」中心付近）は4382.9mだったので、4600mを
閾値にした。この足切りで、巡航中はほぼ毎フレーム26点の判定そのものをせずに抜ける。

### 受け止め方は脚と同じ式だが、2つ調整が要った

1点あたりの力は脚と同じ式（数値的に安定な範囲でいちばん硬いばね）を使うが、
脚とは違う場面で呼ばれるぶん、2つ足す必要があった。

- **質量を点の数で割る。** 割らずに1点あたり機体まるごとの質量ぶんの硬さを
  持たせると、複数の点が同時にめり込んだとき（機体を横倒しにして落とすなど）
  力がそのまま点数倍になる。実測：割らずに試したら、横倒しで3m上から落と
  しただけで0.5秒後に高度727mまで跳ね上がった。
- **めり込みの深さを1mで頭打ちにする。** 脚は構造上センチ単位しかめり込まない
  のでこの心配が要らないが、この判定は機体まるごと・どの姿勢でも呼ばれるので、
  姿勢が一瞬で変わる・急に判定に入るような場面でめり込みが一気に深くなり、
  その1フレームぶんの力が桁外れになる。頭打ちを入れたあとは、同じ横倒し落下で
  0.5秒後に高度16m（+13m）に収まった。それでも弾みはするが、「墜落」自体は
  接触した最初のフレーム（実測7〜9フレーム目）でもう判定済みなので、その後の
  見た目の弾みは副次的なもの。

### そのほか

- 脚以外の点が地面/山に触れたら、Gや沈下率を問わず**即座に墜落**（山への激突は
  `onGround`にもならないので、脚の判定だけでは拾えない）。
- 重心をトリムでずらす（`applyCgOffset`）ときは、メッシュを読み直さず当たり
  判定点をそのまま平行移動する。スライダーのdragごとに呼ばれるので、数万頂点を
  毎回数え直すと引っかかる（メッシュ読み直しを避けている元々の設計と同じ理由）。
- 計算コスト（実測、TB1相当・26点）：巡航高度では広い判定だけで抜けるので
  ほぼゼロ（差-0.001ms/フレーム）、低空で毎フレーム26点ぜんぶ調べても
  +0.02ms/フレーム。
- Builder（`index.html`）はこの当たり判定を使わない——飛行中にしか起きない
  ことなので、`js/env/09d-aircraft-hull.js` は `flight.html` にだけ読み込む。
- まだやっていない：建物・他の機体との当たり判定は無い（地面・山だけ）。

verify-world.js / verify-flight.js（新規テスト込み）/ fleet2.js（6機の着陸）すべて通過。

---

## 00.007.000.004 — ボーンの回転軸をBuilderで手動指定できるようにする

アーカイブ：**`a469349`**（柱 00.007「モデルのボーンを動かす」の最後）

自動判定（板を試しに20°回して、そろい具合・面に垂直な割合からヒンジ軸を選ぶ）は
たいてい合うが、合わないモデルのために逃げ道が要る。Builderの「機体設定」パネルに、
検出した舵の一覧と、ボーンごとのローカルX/Y/Z軸を選ぶ欄を足した。

- `js/env/09c-aircraft-bones.js` を `index.html`（Builder）でも読み込むようにし、
  一覧表示専用の軽い関数 `listBoneAxisCandidates` を追加。試し回転そのもの
  （`pickBoneHingeAxis`）と板の絞り込み（`bonePlateInfo`）は本番の組み立て
  （`buildAircraftBones`）と完全に同じ処理を共有しているので、一覧に出る
  「自動判定：◯軸」は飛行側で実際に使われる軸と必ず一致する。Builder側は編集用の
  小さいシーンなので、飛行側だけに要る `localizeSkeletons`（float32精度対策）は
  呼ばない。
- 指定はボーン名をキーにした軸の上書き（`State.model.boneAxisOverrides`、保存
  データでは `modelBoneAxisOverrides`）。IndexedDB保存・ポータブル設定(.json)の
  両方に載せて復元できる。軸を指定したボーンは、3軸を試す代わりに**その軸だけを
  測り、ヒンジらしさの下限も無視してそのまま使う**——自動では検出できない/
  誤判定されるモデルでも、指定した軸で強制的に動かせる。
- 実測（サンダーバード1号、13候補中の「エルロン」）：自動判定はX軸（ロールの
  符号-1.00）。Y軸へ強制すると `axis=(0,1,0)` に置き換わり、モーメントを
  上書き後の向きから計算し直すのでロールの符号も+1.00に自動で反転した。存在
  しないボーン名を指定しても無視されるだけで、他の12枚の検出結果に影響しない。
  保存→クリア→IndexedDBから再読込でも、選んだプルダウンの値がそのまま戻る
  ことをPlaywrightで確認した。

## 00.007.000.003 — 舵の輪郭が歪む本当の原因（スキニングのfloat32の精度）を直す

アーカイブ：この節（00.007.000）はまだ開いている（節が閉じるときにハッシュを控える）

前の2回（震え対策・Zファイティング対策）では直らなかった。決め手は
「ボーンが付いていない機体本体や尾翼は綺麗」「地上で静止していても歪む」
「動き出すと歪み方が変わる」という報告。

### スキニングだけが、大きなワールド座標を float32 のまま通していた

three.js のスキンは「ボーンのワールド行列 × 逆バインド行列」を `Float32Array` に
詰めてシェーダーへ渡し、**頂点をいったんワールド座標へ運んでから戻す**。この世界は
一辺3000kmあるので機体のワールド座標は数十万に達し、float32 の刻み幅がそのまま
頂点のブレになる。ボーンが付いていないメッシュで起きないのは、そちらは
`modelViewMatrix` をCPU（倍精度）で「カメラからの相対」に直してから渡すため。

実測（シェーダーとまったく同じ手順を float32 で計算し直して倍精度と比べた。単位m）：

| 機体のワールド座標 | 直す前 | 直した後 |
|---|---|---|
| 原点 | 0.00002 | **0** |
| x=53,073 | 0.0034 | **0** |
| x=200,000 | 0.0085 | **0** |
| x=600,000 | 0.0329 | **0** |
| x=1,400,000（世界の端） | 0.0673 | **0** |

翼の縁や細い塗り分けの線は数cmしか幅がないので、6.7cmのブレは線が裂けて見える。
実機と同じレンダラー設定で世界の端に置いて撮ると、直す前は白い縁取りがちぎれ、
直した後は一本の線にきれいに揃った。

### 直し方：ボーンをモデルのローカル座標へ移す

ボーンを機体の入れ物（ワールドに置かれている）から外し、モデルのローカル座標の
ままの入れ物へ移した（`localizeSkeletons`）。`bindMode` を `'detached'` にして、
バインド行列に「モデルの中でのメッシュの位置」を入れ、そのぶんを逆バインドボーン
行列側から抜いてある。機体のワールド位置はこれまでどおり `modelViewMatrix` 側で
掛かるので**見え方は変わらない**——実測でも、ローカル化の前後で頂点のワールド位置の
差は**0**（完全一致）、舵の効き（モーメント）も前回と同じ値だった。

### Zファイティング対策は戻した

前回入れた「舵の板を3.5cm持ち上げる」対策（`BONE_NUDGE_M`）は削除した。舵面が
親の翼とぴったり重なっているのは事実だが、輪郭が歪んで見えていた原因はそちらでは
なかったので、モデルを勝手にずらすのはやめた。

verify-world.js / verify-flight.js / 6機の着陸すべて通過。

---

## 00.007.000.002 — 舵の輪郭が裂ける不具合の原因をZファイティングと判断して対策した（後で取り消し）

アーカイブ：この節（00.007.000）はまだ開いている（節が閉じるときにハッシュを控える）

前回の震え対策（00.007.000.001）だけでは直らず、「地上で完全に静止していても
歪む」という報告を受けて調べ直した。**震えているのではなく、輪郭がZファイティングで
裂けていた。**

### 舵面のメッシュは、親の翼とぴったり重ねて作られている

エルロン/スポイラー/フラップのメッシュは、たいてい親の翼メッシュと同じ場所に
重ねて置かれている。実測（頂点どうしの最短距離）：エルロンは頂点の**100%**、
スポイラー/フラップも**67〜86%**が主翼側の頂点と距離0.01未満——ほぼ同じ場所。

このシーンは対数深度バッファ（`logarithmicDepthBuffer: true`）を使っていて、
**`polygonOffset` がほとんど効かない**（着陸灯の板のコードに前から同じ注意書きが
あった）。重なった2枚の面はどちらが手前か毎フレーム決まらず、境界がギザギザに
裂けて見える——舵を1mmも動かしていなくても、地上で静止していても起きる。
実機と同じレンダラー設定で境界をすれすれの角度から撮ると、直す前は白い縁取りが
破線状にギザギザに裂け、直したあとは一本の線にきれいに揃った。

### 直し方：舵の板を厚みの向きへ3.5cm持ち上げて重なりを消す

いちばん近い主翼側の頂点の塊から遠ざかる向きへ、ボーンの位置をわずかに動かす
（`BONE_NUDGE_M`）。実測（スキニングを通した実際の位置で測り直した）：

| | 直す前 | 直した後 |
|---|---|---|
| エルロン（主翼と距離0.01未満の割合） | 100% | 6.3%（ヒンジの付け根のみ） |
| スポイラー | 74.5% | 0% |
| フラップ | 66.7% | 0% |

3.5cmは機体全長35mの0.1%ほどで、舵の効き（モーメントの向き・大きさ）は前回と
完全に同じ。

測定でもう一つ落とし穴を踏んだ：最初「頂点属性の生の位置」で重なりを調べたところ、
持ち上げたあとも数値が0のまま変わらないという誤った結果が出た——生の頂点属性は
ボーンを動かしても変わらない（ヒンジ軸を測るときに踏んだのと同じ落とし穴）。
**スキニングを通した位置**で測り直して、直っていることを確認した。

verify-world.js / verify-flight.js / 6機の着陸すべて通過。

---

## 00.007.000.001 — 舵の輪郭が細かく震える不具合を直す

アーカイブ：この節（00.007.000）はまだ開いている（節が閉じるときにハッシュを控える）

「ボーンの対象の羽の輪郭が常にプルプルしてる」という指摘を受けて直した。

### 原因は天候の突風

天候の突風（`w.gust`）はなめらかに揺れ続けるので、巡航中でも自動操縦の舵は
フレームごとにほんのわずかずつ動き続ける——実測で最大**0.075°/フレーム**。
これを毎フレームそのままボーンに反映していたので、舵の輪郭がずっと細かく
震えて見えていた。ヘッドレスの物理だけのテスト（無風）では気づけず、突風を
足して初めて再現できた。

### 直し方：見た目の更新をしきい値で間引く

内部の角度は毎フレーム動かし続けるが、**実際にメッシュへ反映するのは前回の
見た目から0.12°ぶん動いたときだけ**にした（`BONE_DEADBAND_RAD`）。実測で、
同じ揺れの入力に対してメッシュを実際に書き換える頻度が**100%→1.1%**まで落ち、
1回あたりの動きも0.13°ほど（見えない大きさ）に収まった。本物の操縦（フル舵で
28°）はこの何十倍も大きく動くので、反応が遅れて感じることはない。

副作用として毎コマの更新コストも0.0083ms→0.0045msに下がった（更新そのものを
間引いているので）。舵の効き（モーメントの向き・大きさ）は前回と完全に同じ。

verify-world.js / verify-flight.js / 6機の着陸すべて通過。

---

## 00.007.000.000 — GLBのボーンで舵面が動くようにする

アーカイブ：この節（00.007.000）はまだ開いている（節が閉じるときにハッシュを控える）

Blenderで舵にボーンを仕込んだGLBを読み込むと、操縦に合わせてエルロン・
エレベーター・ラダー・フラップ・スポイラーが実際に動くようにした。**見た目だけ**で、
飛び方はこれまでどおりBuilderで置いたパーツが決める。

### 名前ではなく、形と動きから役割を決める

ボーン名は「右上」「左下」のように位置しか言っていないことが多く、
サンダーバード1号のモデルでは親の名前（エレベーター／ラダー）も実際の働きと
合っていなかった（親が「ラダー」の板を測ったらピッチに効く板だった）。
そこでボーンを試しに20°回し、動いた頂点から

1. **ヒンジ軸** … そろい具合（板が同じ向きに動くか）×面に垂直な割合が最大で、
   いちばん大きく振れる軸
2. **役割** … 板が動いた逆向きに空気が押す力の、重心まわりのモーメント
3. **担当する軸** … 軸ごとに「最上位の舵と比べた順位」を取り、自分がいちばん
   得意な軸の0.7に届かない軸は担当しない

を決める。3.が要るのは、エルロン1枚のモーメントが実はロール(腕6.9)より
ピッチ(腕9.5)のほうが大きく、単体で見るとピッチ用に見えてしまうため。
この作りだとV字尾翼や全翼機のエレボン（2軸とも自分が1位）は自動的に混ざる。

実測（サンダーバード1号・12枚。操縦を入れて実際に動いた頂点からモーメントを積む。
正＝機首上げ／右ロール／機首右）：

| 操縦 | 機首上げ | 右ロール | 機首右 |
|---|---|---|---|
| 引く | **+29.9** | 3.6 | -12.6 |
| 押す | **-30.8** | -3.7 | 0.7 |
| 右ロール | 0.0 | **+5.3** | 0.0 |
| 右ヨー | -5.9 | 0.9 | **+72.0** |
| フラップ全開 | -5.2 | 0.0 | 0.0（板は下へ） |
| スポイラー全開 | +8.4 | 0.0 | 0.0（板は上へ） |

### 測り方の落とし穴が2つ

- 頂点属性の位置を基準にすると、**ファイルの姿勢がバインドポーズと違う**機体で
  壊れる。この尾部は親に scale -1 が掛かっていて最大12.5（全長34.9の1/3）ずれ、
  「20°回したら板が12動いた」ことになっていた → 基準もスキニングを通した位置にした
- 「長手方向に空きがあれば離れた2枚だ」で外そうとしたら、**まともな舵が全部
  弾かれた**（12枚→0枚）。低ポリの板は前縁と後縁にしか頂点が無く、空きが94%
  あるのが普通だった → 大きさの外れ値（中央値の3倍超）で外す方式に変更

手間は組み立て時に1回2.9ms、毎コマ0.0083ms（60fpsの1コマの0.05%）。
内蔵機やボーンの無いGLBでは何も起きない（実機確認済み）。
着陸脚と可変翼のボーンはまだ動かしていない（脚は棒なのでヒンジが形から決まらず、
可変翼は見た目だけ動かすと物理と食い違うため）。

verify-world.js / verify-flight.js / 6機の着陸すべて通過。

---

## 00.006.000.004 — 雷を連打に、ブームを胴鳴り付きに、雨を控えめにする

アーカイブ：**`09aa846`**（柱 00.006「音」の最後）

前回追加した雷・ブーム・雨について、実際に鳴らした感想から3つ直した。

### 雷が「ガンドン」という金属的な音に聞こえていた

1〜2発の「クラック→ゆるい唸り」だけの作りだったのを、短く切った「ドン」を
不規則な間隔（詰まった連打・ふつうの間・長い間）で何発も鳴らし、だんだん
弱くする作りに直した。個々の「ドン」の減衰も0.4〜1.3秒→**0.09〜0.18秒**に
大きく短縮してパーカッシブにし、下に低い唸りの土台を1本敷いて連打の後の
「………」を受け持たせた。

実測（包絡線の山＝「ドン」の数の目安）：

| 距離 | 尖頭 | 続く秒数 | ドンの数(目安) |
|---|---|---|---|
| 500m | 0.98 | 1.16s | 5（設計どおり4〜6発） |
| 3,000m | 0.31 | 3.16s | 4（設計どおり3〜5発） |
| 9,000m | 0.10 | 7.9s | 測定不能（低い唸りに埋もれる。後述） |

近い/中距離はきれいに数えられたが、9,000mは「山を数える」測り方が雑音の
揺らぎを拾ってしまい当てにならなかった——測定側の限界として書き残す。
帯域パワー比（中高/低）は前回の実測（500m 0.662／3,000m 0.517／
9,000m 0.0043）から変えていない。

### ブームが「パンッ」で終わり、迫力が無かった

一瞬のクラックだけで消える1層構成だったのを、①クラック＋②遅れて膨らみ
ゆっくり沈む低い胴鳴りの2層に分け、「どおぉぉぉぉ…ん」という長い響きにした。

実測（onsetからの経過時間ごとのRMS）：

| 経過 | 0ms | 20ms | 220ms(胴鳴りの山) | 700ms | 1,400ms | 1,800ms |
|---|---|---|---|---|---|---|
| RMS | 0.181 | 0.194 | **0.369** | 0.045 | 0.002 | 0.000 |

胴鳴りが220ms付近でクラック自体より大きく膨らみ（0.369 > 0.194）、そこから
1.8秒かけて沈む。実機でも、マッハ1を切った直後のRMSが0.008→**0.163**へ
（前は0.006→0.081）、立ち上がりの音量が約2倍になった。

### 雨が雷・ブームに対して大きすぎた

`SOUND_RAIN_GAIN` を 0.9→**0.4** に。実測（土砂降り全開RMS ／ 距離0の
ブーム尖頭 ／ 1,000mの雷尖頭）：雨0.056 に対してブーム0.834・雷1.165、
背景の雨と一瞬の出来事である雷・ブームの音量差がはっきり出るようにした。

verify-world.js / verify-flight.js / 6機の着陸すべて通過（音のみの変更）。

---

## 00.006.000.003 — 風切音・ソニックブーム・雨と雷を足す

アーカイブ：この節（00.006.000）はまだ開いている（節が閉じるときにハッシュを控える）

音かな、と言われていた残り4つ（風切音・ソニックブーム・雨・雷）をまとめて実装。

### 風切音は動圧（q=½ρv²）で鳴らす

対気速度そのものではなく、高度の空気の薄さ（`airDensityAt`）まで含めた動圧を
基準速度170m/s・海面の動圧で正規化し0.62乗で圧縮。速度が上がるほど音量も
バンドパスの中心周波数も上がる。

- 実測（`OfflineAudioContext`、海面、距離0）：0m/s RMS0.000 → 30m/s 0.012 →
  80m/s 0.040 → 130m/s 0.067 → 170m/s 0.091 → 260m/s 0.145（単調増加）
- 実機（フィルタの中心周波数を直接読む）：0m/s 266Hz → 260m/s 3,509Hz
- 同じ対気速度170m/sでも、海面RMS0.090 → 高度11km RMS0.046（比0.51）。
  空気密度比(0.298)の0.62乗＝理論値0.47に近い

### ソニックブームは「マッハ1を超えた瞬間」ではなく「マッハ円錐が観測者を掃く瞬間」

自由視点（耳が世界に固定）では毎フレーム「観測者への向き」と「機体の後ろ向き」の
なす角をマッハ角μ=asin(1/M)と比べ、円錐の外→内に変わった瞬間に鳴らす。同乗視点は
既存の衝撃波の見た目（気化雲の輪）と同じ、マッハ1を切った瞬間に1回。

**教科書の閉じた式と突き合わせて検証**（今回いちばんの収穫）。飛行経路の真下の
観測者に届く遅れ `t = h・√(M²−1) / (M・c)` と、実際に機体を飛ばして円錐判定が
発火する時刻を比べた：

| 高度 | マッハ | 実測遅れ(s) | 予測遅れ(s) | 誤差(s) |
|---|---|---|---|---|
| 3,000m | 1.3 | 5.835 | 5.834 | 0.0005 |
| 3,000m | 2.0 | 7.910 | 7.907 | 0.0031 |
| 3,000m | 3.0 | 8.610 | 8.608 | 0.0013 |
| 8,000m | 1.3 | 16.596 | 16.594 | 0.0017 |
| 8,000m | 2.0 | 22.492 | 22.490 | 0.0015 |
| 8,000m | 3.0 | 24.488 | 24.485 | 0.0031 |

6通りすべて誤差1/300秒以内。実機での確認（同乗視点、じわじわ加速）でも
マッハ1を切った直後（mach=1.002）にRMSが0.006→0.081へ跳ね上がった。

### 雨は非位置的、雷は本物の音速遅れ

雨は `EnvState.weather.current`（すでにカメラ位置で評価済み）をそのまま読むだけ。
雷鳴は光ってから聞こえるまでをポーリングで数えるのではなく、距離÷音速(340m/s)を
Web Audioの`start(when)`に直接渡して予約——フレームレートが乱れても正確に鳴る。

実測（距離ごとの中高音(300-3000Hz)÷低音(20-300Hz)の帯域パワー比）：

| 距離 | 尖頭 | 続く秒数 | 中高/低の比 |
|---|---|---|---|
| 500m | 1.13 | 0.71s | 0.662 |
| 3,000m | 0.66 | 1.80s | 0.517 |
| 9,000m | 0.16 | 3.34s | 0.0043 |

測り方の落とし穴が2つ：①いちばん強い1点を探す測り方は雑音の低域偏りで常に
ほぼ直流を拾ってしまう→帯域パワーの比に変更。②固定の絶対時刻でスペクトルを
切り出すと、遠い雷鳴はattackが長い(0.015s〜0.18s)ため無音を測ってしまう→
onsetの250ms後（どの距離でも攻撃を終えている時刻）に変更。

「飛ぶ」を押す前（環境プレビューのまま）でも雨と雷が鳴ることを確認：
RMS clear 0.000 → rain 0.051 → storm 0.081 → snow 0.000（雪は正しく無音）。

verify-world.js / verify-flight.js / 6機の着陸すべて通過（音のみの変更なので
飛行物理・地形には触れていない）。

---

## 00.006.000.002 — ジェットの「ピー」を「キーン」にし、ズームで音程が動くのを直す

アーカイブ：この節（00.006.000）はまだ開いている（節が閉じるときにハッシュを控える）

### ジェットのキーンが笛になっていた

三角波を1本鳴らしていたので、それはもう純音＝笛（ピー）だった。実機のファン音は、
翼通過の基音とその倍音に、同じ高さの狭い帯域の雑音（羽根の間の流れの乱れ）が
乗った「かすれた」音。

- 基音＋2倍音＋3倍音に、同じ高さの狭帯域雑音を2本重ねた

実測（山のするどさ ＝ いちばん高い点 ÷ その帯域の中央値。大きいほど笛に近い）：

| | 前 | 後 |
|---|---|---|
| 1.5k〜5kHz | **8,090倍** | **118倍** |
| 2k〜4kHz | 6,736倍 | **48倍** |
| （参考）ロケットの広い帯域 | 2倍 | 2倍 |

- 山の位置（2,998Hz）は変えていないので、音程は分かるまま「かすれ」だけが増える
- ジェット全体でいちばん強い山も 2,998Hz → **291Hz**（排気のゴー）へ移り、
  高音(1.5k-5k)の割合は 21.4% → 9.0% に

### ズームでも音程が動いていた

前回「距離そのものの変化率」にしたので、**ズームで距離が変われば音程も動いて**いた
（指で寄っただけなのに）。ドップラーはここまでで**3回作り直している**：

| 作り | スワイプ | ズーム |
|---|---|---|
| ① 機体の速度 − カメラの速度 | 2.91半音 | 21.46半音 |
| ② 機体の速度だけ | 14.05半音（悪化） | — |
| ③ 距離そのものの変化率 | 0.32半音 | **21.46半音** |
| ④ **カメラが何に繋がれているか**（いま） | **0.00半音** | **0.00半音** |

- ④の考え方：見かけの動きではなく**耳が乗っている乗り物の速度**を使う。
  追従・機体固定・コックピット・周回はカメラが機体に繋がれているので相対速度は0
  （同乗していれば自分のエンジンの音程は変わらない）。自由視点だけは世界に
  置いてあるので、機体の速度の視線方向の成分がそのままドップラーになる
- **本物のドップラーは残っている**。自由視点で止まって見ている150m横を160m/sで
  通過させると、比が 1.88 → **1.10（通過の瞬間）** → 0.69 と滑らかに動く
- 残っている限界：自由視点でカメラを機体のまわりに回すように振ると、視線の向きが
  変わるぶん音程が動く（11.02半音）。その位置に立っている人には実際そう聞こえるので
  物理的には正しい。飛ぶときに使う4つの視点は0.00半音なのでそのままにしてある

音の作り全体は崩れていない（プロペラ0.184/97Hz、ジェット0.122、AB0.162、
ロケット0.259。混ぜても尖頭0.884・1.0に張り付く画素0%）。
verify-world.js / verify-flight.js / 6機の着陸すべて通過。

---

## 00.006.000.001 — 視点をスワイプしたときの音のグワグワを直す

ドップラーを「機体の速度 − カメラの速度」から出していたので、**スワイプで視点を
回しただけで耳が秒速数百mで飛んでいることになり**、音程が揺れていた。
ジェットで特に目立つのは、ファンのキーンが**純音**だから（雑音には音程が無いので
揺れても分からない。ロケットやプロペラで気にならなかったのはそのため）。

- **定義に戻して、距離そのものの変化率から出すようにした。** ドップラーは
  「音源と耳の距離が縮まる速さ」で決まるのだから、距離を毎フレーム引き算すればいい。
  すると「半径を保ったまま回すスワイプ＝距離は変わらない」「機体に付いた視点＝
  距離は一定」「横を通過する機体＝距離が縮んでから伸びる」が式ひとつで同時に成り立つ
- ワープ（Rで滑走路へ戻すなど）で距離が飛んだときは、速度と読まずに0へ戻す。
  そのままだと音速を超えた偽のドップラーが「ヒュウン」と鳴る
- 比の変化に1秒あたりの上限を足した（真横を通り過ぎる瞬間の段差を消す）

実測（ジェット機を等速で飛ばしたまま、1秒で半周スワイプしたときの音程の揺れ）：

| | 前 | 後 |
|---|---|---|
| 自由視点 | **2.91半音** | **0.32半音** |
| 追従視点 | 2.91半音 | **0.32半音** |
| 周回視点 | 2.91半音 | **0.32半音** |

- 途中、カメラの速度をやめて機体の速度だけにする案も試したが、離れた視点で
  **14.05半音**とかえって酷くなった（半周まわるあいだに視線の向きが速度ベクトルを
  またぐので、近づく成分が+160から-160まで振れる）ので捨てた
- **本物のドップラーは残っている**。止まって見ている150m横を160m/sで通過させると、
  比が 1.88（接近）→ **1.11（通過の瞬間）** → 0.69（遠ざかる）と滑らかに動く
  （理論値は接近1.889・遠ざかり0.68）

---

## 00.006.000.000 — 音（柱6本目）：エンジン各種の音

**柱が1本増えた。** 音はこれまで一切なかった仕組みなので、柱を +1 して
00.006 から始める（前の柱 00.005 の節は `acf7be9` で閉じた。下を参照）。

- **音のファイルは持たない。** エンジン種別×出力×距離のぶんだけ音源を用意する
  かわりに、Web Audio の発振器と雑音から**その場で合成する**。地形やテクスチャを
  その場で作っているのと同じやり方で、出力や距離に合わせて連続して変わる
- 種別ごとに「何が鳴っているか」を変えてある
  - プロペラ … 羽根が空気を叩く音（翼通過周波数）。出力で25〜95Hzを行き来する
  - ジェット … 低い「ゴー」（排気の雑音）＋高い「キーン」（ファンの翼通過音）
  - AB付き … ジェットに、点いたときだけ腹に来る低い唸りを足す
  - ロケット … ほとんど低い雑音
- 距離で小さくなり、遠いほど高い音から先に減る（空気の吸収）
- ドップラー。近づけば高く、遠ざかれば低く
- 音量つまみと入り切りを環境設定に足した（保存される）

実測（OfflineAudioContext で実際にレンダリングして、FFTで帯域ごとに測った）。
全開・目の前：

| 種別 | RMS | 尖頭 | いちばん強い山 | 20-150Hz | 1.5k-5kHz |
|---|---|---|---|---|---|
| プロペラ | 0.184 | 0.50 | **97 Hz** | 66.9% | 0.1% |
| ジェット | 0.130 | 0.51 | **2,998 Hz** | 12.1% | 21.4% |
| AB付き | 0.164 | 0.69 | 3,198 Hz | 48.4% | 13.7% |
| ロケット | 0.252 | 0.94 | **65 Hz** | 73.2% | 0.5% |

- プロペラの基音は出力0.05/0.25/0.5/1.0 で **27/43/59/97 Hz**（設計どおり）
- ジェットのキーンは同じく **813/1,276/1,852/2,998 Hz**
- 距離：0m で RMS 0.130 → 30m で 0.066（ちょうど半分）→ 1km で 0.0036 →
  3km で 0.0011。高音/中低音の比は 0.46 → 1km で 0.20 → 3km で 0.00007
- ドップラー比 0.7 / 1.0 / 1.4 で、プロペラの山が **65 / 97 / 135 Hz**
- ロケット4基＋AB2基を20mで同時に鳴らしても、尖頭0.861・**1.0に張り付く画素0%**
- `updateSound` の1コマは **0.0078ms**（60fpsの1コマの0.05%）

途中で見つけて直したこと：

- **波形が1.0を超えて割れていた**（ロケット全開で尖頭2.48）。しかもエネルギーの
  山が**22Hz**＝ほとんど聞こえない超低音に溜まっていた。45Hzで切り、
  マスターにリミッターを入れた
- 帯域を「16点のDFT」で測っていたので、**プロペラの基音を点と点のあいだに
  落として取りこぼしていた**（低音62.6%と出るはずが12.6%と出た）。FFTに直した
- `updateSound` の入口で「まだ音を組んでいなければ何もしない」と書いていたので、
  誰も組まず**永久に鳴らなかった**。組むのはその関数の仕事なので、入口を直した

---

## 00.005.000.009 — 曇り・雷雨の夜、地平線が明るすぎるのを直す

アーカイブ：**`acf7be9`**（柱 00.005「見た目の作り込み」の最後。
`git checkout acf7be9` でこの版がそのまま動く）

- 曇りの灰色は「昼の色」なのに、夜にもそのまま混ぜていた。だから夜の地平線が
  昼の曇り空と同じ明るい灰色になっていた
- 雲は自分では光らないので、曇った夜は晴れた夜より**暗い**（月も星も隠れる）。
  昼の灰色と夜の灰色を用意して、昼夜でその間を取るようにした
- 散乱光の底上げ（`ambientFactor`）も昼だけにした。夜は逆に、雲が月と星を
  ふさぐぶん暗くする
- 稲光は昼夜で変えない（それが稲光なので）

実測（深夜1時・対地1,500mから水平線を正面に見て、画面上半分の平均の明るさ）：

| 天気 | 前 | 後 | 霧色（前→後） |
|---|---|---|---|
| 快晴 | 23.8 | 23.3 | #08101d → #08101d |
| 曇り | 41.1 | **14.5** | #303844 → #09101b |
| 雨 | 67.9 | **19.2** | #4d555f → #0a0f19 |
| 雷雨 | 68.8 | **17.0** | #555d67 → #090e16 |
| 雪 | 76.1 | **28.3** | #636b74 → #090e16 |
| 霧 | 105.7 | **46.6** | #818991 → #0a0e15 |

- 昼の見た目は変えていない（正午の空の明るさは前後とも225〜253で、天気の
  移り変わりのゆらぎの範囲）
- 嵐の夜の空の明るさは、稲光なし21.8 → 半分72.6 → 全開123.7。暗くしても
  稲光はちゃんと目立つ
- 嵐の夜の進入（滑走路手前2km・対地150m）で、明るい画素の割合が
  着陸灯オフ1.4% → オン3.8%。滑走路は問題なく見える

---

## 00.005.000.008 — 湿った空・雨・雪・霧で、着陸灯の光の筋が見える

- 光線が見えるのは、空気に光を散らすものがあるときだけ。晴れて乾いた空では
  出さない。濃さは 降り＋霧＋もや から決める（雪は粒が大きいので1.35倍）
- 形は側面だけの円錐1枚。濃さの変化は頂点色に入れてある
- 手前（灯りのすぐそば）は必ず消す。コックピット視点のカメラは灯りのすぐ後ろに
  いるので、根元まで濃いと画面がまっ白になる
- 筋は視程の55%より長くは引かない

実測（対地90m・夜・横から見て、筋のあり／なしで画面の明るさを引き算）：

| 天気 | 散らす強さ | 筋の濃さ | 画面が明るくなるぶん（中央） |
|---|---|---|---|
| 快晴 | 0 | 出ない | 0.0 |
| 曇り | 0 | 出ない | 0.0 |
| 雨 | 0.38 | 0.118 | +10.2 |
| 雷雨 | 0.77 | 0.237 | +21.3 |
| 雪 | 0.88 | 0.300 | +26.9 |
| 霧 | 0.93 | 0.278 | +25.0 |

- 霧の中・夜・着陸灯オンで、コックピット／すぐ後ろ／追従のどの視点でも
  真っ白な画素は**0%**
- 真昼の濃い霧では濃さ0.027まで落ちる（0にはしない。濃い霧は太陽も暗くするので、
  実機でも昼の光条がうっすら見える）

---

## 00.005.000.007 — 着陸灯を、付けた場所と向けた角度どおりに照らす

- 以前は機体に光の板を**1枚だけ**置き、機体の位置から機首の向きへ、
  伏せ角11°の決め打ちで照らしていた。灯りをどこに付けても、どちらへ向けても、
  出る光は同じ場所だった
- いまは**灯り1つにつき板1枚**を、その灯りの世界での位置と向きから置く。
  向きはパーツの回転そのもの（エンジンの推力と同じ約束）
- 機体の傾きもそのまま入る（頭を下げれば近くへ、バンクすれば横へ振れる）
- Builderに「伏せ角」「広がり・半角」「届く距離」を足し、着陸灯のギズモを
  **円錐**（頂点が灯り・底面が照らす先）にした

実測（機首中央・左翼(左へ25°)・右翼(右へ25°)の3灯、対地60m。
各灯の光線が地面に当たる点が、描かれている筋の中心線から横へ外れる距離）：

| | 前 | 後 |
|---|---|---|
| 板の数 | 1枚（3灯なのに） | **3枚** |
| 機首（振っていない） | 0 m | 0 m |
| 左翼（左へ25°） | **178.6 m** | **0 m** |
| 右翼（右へ25°） | **178.5 m** | **0 m** |

- 頭を8°下げると、光の当たる点が421m先 → **300m先**へ寄る
- 20°バンクさせると、上を向いた側の灯りは地面に届かなくなり**板が2枚に減る**
  （残った側は205m先へ寄る）

---

## 00.005.000.006 — 煙が地面や海を突き抜けず、面に当たったら広がる

- ロケットの煙とタイヤの煙が、地面・海面・湖面を突き抜けて塗られていたのをやめた。
  粒は「板」なので、中心が面の上にあっても半径のぶんは面の下まで塗られる
  （半透明の板は深度を書かないので、滑走路の上にべったり白い円が乗って見えた）
- 面に当たった粒は、下向きの勢いを**外向き**に振り替えて横へ広がる。
  面を這っているあいだは抵抗を 1.6→0.45 に下げて、遠くまで流れるようにした
- 面の高さは置いたときに1回だけ引く（エンジン1基につき毎フレーム1回）。
  そのうえで、面のそばにいる粒を毎フレーム最大12個だけ引き直して斜面に追従させる

実測（上向きロケット1基のVTOL機を滑走路の上でホバリング。
生きている粒のうち、板の下端が滑走路より下にあるものの割合）：

| 対地 | 前 | 後 | いちばん深いところ |
|---|---|---|---|
| 2m | 91.7% | **0%** | 8.19m → 0m |
| 6m | 51.2% | **0%** | 4.30m → 0m |
| 20m | 0% | 0% | 届かない（変化なし） |

- 海の上（水深2,625mの沖）でも、海面(y=0)より下に塗られる粒は0%
- 広がりぐあい：対地2mのとき、煙が届く横の距離が 2.0m → **3.5m**（1.75倍）
- 値段：`updateRocketSmoke` の1コマが 0.023ms → **0.047ms**（60fpsの1コマの0.3%）。
  粒ごとに毎フレーム面の高さを引くと2000回で6.4ms（1コマの4割）かかる

---

## 00.005.000.005 — 雲底のまるい模様をやめる

- 曇り空の天井の模様を、サイン波6本の重ね合わせから、継ぎ目なくタイルする
  格子の値ノイズ6段に変えた。正弦は山も谷もまるいので、45°の格子にまるいしみが
  並んだ模様になっていた（「雲底がまんまるに見える」の正体）
- 模様の濃淡（標準偏差）は 20.4 → 37.7、いちばん暗いところは 150 → 120
- タイル1枚の実寸を 26km → 9km に（26kmだと雲底の下からのっぺりして見えた）
- テクスチャを作るのにかかる時間は58.7ms（起動時に1回だけ）

---

## 00.005.000.004 — 垂直離陸のあと、速度が乗るまで曲がらない

- 前へ進むエンジンへ切り替えた瞬間から目的地の方角へ舵を当てていたのをやめた。
  ほぼ止まっている機体がバンクすると、傾くのは垂直エンジンの推力の向きだけで、
  支えを横へ逃がすことになる
- 切替中の高さを「その高度を目標にした輪」で保つようにした（開ループだった）
- 翼が支えられる速さになってから、垂直エンジンを12秒かけて抜く。
  抜けきってから上昇の段へ渡す（速度だけで渡すと支えが一度に消える）
- 切り替える高さを対地30m→60mに上げた

実測（目的地が真後ろ・全自動で離陸から着陸まで）：

| 機体 | 最大G | 渡したあとのいちばん低い対地 |
|---|---|---|
| サンダーバード1号 | 34.3G → **1.8G** | -8m → **60m** |
| サンダーバード2号 | 10.4G → **2.5G** | -5m → **60m** |

---

## 00.005.000.003 — 煙の筋を連続にする

- 粒を「毎秒何個」ではなく**進んだ距離ごと**に置くようにした。時間で置くと
  間隔が速度に比例して開き、速い機体では点線になっていた
  （実測：マッハ2で粒の間隔19.8mに対し粒の直径2.0m）
- 粒の数で描ける長さに合わせて、速いときは寿命のほうを縮める
  （マッハ2で寿命3.0秒・筋の長さ1,867m）
- 粒を1基あたり900→2,000に増やした（コマ時間はほぼ変わらず：8.6 vs 8.8fps）
- 隣との隙間÷粒の直径は **100kt:0.48 / マッハ1:0.58 / マッハ2:0.56**。
  1を下回っていれば粒どうしが重なっている（直す前のマッハ2は9.9）

---

## 00.005.000.002 — 煙を大きくし、赤いところを距離で決める

- 炎の照り返しが抜けるのを、時間ではなく**ノズルから離れた距離**で決めるようにした。
  実測（ノズル半径0.13m）で、赤い範囲がマッハ2のとき **295m → 50m**。
  100ktでは 26m → 13m で、遅い機体の見えかたはほぼそのまま
- 走行距離から出し直す形にしたので、コマ落ちしていても赤い範囲が変わらない
  （毎秒15コマの環境では、前の書き方だと赤い粒が常に0個だった）
- 煙を大きくした。出たて1.15倍・膨らんだあと1.56倍（15→150倍）。
  真後ろ70mから見た真っ白な画素は 5.9% → **6.7%**（両方1.4倍にすると20.3%）
- 煙が噴射で後ろへ押し出されるようにした（出力に比例。抵抗ですぐ止まる）

---

## 00.005.000.001 — 煙の見えかたを直す

- 半透明を描く順番を1か所（`ENV_ORDER`）にまとめた。排煙と地名ラベルが
  海の上・雲底の下で塗りつぶされて消えていたのを直した
- 煙がまわりの明るさに従うようにした（夜に白く光って見えていた）。霧も掛かる
- 煙がノズルの出口で炎に赤く照らされるようにした
- 点いてはいるが0%のエンジンから煙が出ていたのを直した
  （実測：レバー45%・ロケット0%で粒0個。直す前は出ていた）
- 「水平線の位置で炎が見えない」は、測った範囲では再現できなかった（下の注を参照）

**再現できなかったもの**：炎を消した絵との差で測ると、水平線をまたいだ炎は
空側（明るい霞が背景）で変化の最大199、海側で460——薄くはなるが消えてはいない。
加算で描いている以上、白に近い背景では弱くなるのは避けられない。芯をふつうの
重ねにする案と、身を1枚足す案の両方を実際に入れて測ったが、空側は199→195／191と
ほとんど変わらず、炎の濃さだけが変わってしまうので戻した。
排煙が海の上で消えていたぶん（上の1つめ）が見えていたのかもしれない。

---

## 00.005.000.000 — 着陸まわりを作り直す

バージョン名を付けはじめた地点。この版そのものは、下の2つの変更を含む。

### 垂直着陸を「対地200mで静止してから降りる」手順にする

段を `vtol_approach → vtol_hover → vtol_descent` に分けた。それまでは
滑走路の進入と同じ「寄せながら降りる」だけで、止まる算段がどこにも無かった。

- 実測（サンダーバード1号 NOVA→NOIS・全自動）：着地点の8.0km手前・389kt・
  沈下24m/sのまま切り替わり、**着地点から8,668m先の地面へ3,097fpm・18.9Gで
  突っ込んで裏返しに滑っていった** → **-54fpm・1.4G・着地点から4m**
- THCA→ORKU（2,877km）：-57fpm・2.0G・7m
- サンダーバード2号 NOVA→NOIS：降下の段から45分出られず → -59fpm・1.7G・0m
- 空中の逆噴射を、垂直離着陸用エンジンを持つ機体にだけ開いた
- 逆推力を「いま回っているグループのぶん」で数えるようにした
  （TB1は全機ぶん73.4m/s²に対し、進入で回っている群だけだと5.63m/s²）
- いちばん遅いグループで進入速度が出せない機体では、そこまで落とさない

### 接地の衝撃を直し、タイヤの煙を足す

- 脚のばねを**縮むほど硬くなる**ものにした（設計荷重を静止時の沈み込みで
  割って一本調子にしていたので、推力の大きい機体の脚が桁違いに硬かった）
- 減衰を沈み込んだぶんだけ効かせる（最初の1フレームだけ棘になっていた）
- 着陸滑走でフラップを全開にするのをやめた（接地直後に浮き上がっていた）
- 着陸滑走では、速いうちは機首を少し上げたまま保つ

| 機体 | 接地G 前 | 後 |
|---|---|---|
| 内蔵の練習機 | 3.4 | 3.1 |
| 三式戦闘機 | 2.5 | 2.0 |
| Boeing 747 | 10.7 | **1.7** |
| Concorde | 6.5 | 6.0 |
| サンダーバード2号 | **20.4** | **3.9** |
| サンダーバード1号 | 11.0 | 12.2 |
| 1号・2号（垂直着陸） | 1.8 / 1.7 | 1.1 / 1.1 |

- 接地したときにタイヤから煙が上がるようにした

---

## 番号を付ける前（00.001 〜 00.004）

バージョン名を付けはじめる前に立っていた柱を、さかのぼって数えたもの。
1つ1つの変更に番号は振っていない（Gitの履歴を参照）。

### 00.004 — エンジンの種別と排気の見た目
エンジンにプロペラ／ジェット／アフターバーナー付き／ロケットの種別。
グループ分けと段階起動、飛行機雲、ソニックブーム、炎とロケットの煙、
ノズルの大きさと向きの設定、航行灯と着陸灯。

### 00.003 — 自動操縦
離陸から着陸までの全自動、高度維持、地形回避、スポイラーと逆噴射、
垂直離着陸のオンオフ、進入・引き起こし・着陸滑走。

### 00.002 — 飛行
翼ごとの空力、舵面、脚の接地、重心と釣り合い、視点と計器、
タッチ操作、Builderで組んだ機体をそのまま飛ばす仕組み。

### 00.001 — 世界
3,000kmの地形・海・気候帯・都市・空港・川と湖・植生・天候・ミニマップ。
